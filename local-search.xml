<?xml version="1.0" encoding="utf-8"?>
<search>
  
  
  
  <entry>
    <title>Spring事务传播行为与失效场景</title>
    <link href="/2026/03/09/Spring%E4%BA%8B%E5%8A%A1%E4%BC%A0%E6%92%AD%E8%A1%8C%E4%B8%BA%E4%B8%8E%E5%A4%B1%E6%95%88%E5%9C%BA%E6%99%AF/"/>
    <url>/2026/03/09/Spring%E4%BA%8B%E5%8A%A1%E4%BC%A0%E6%92%AD%E8%A1%8C%E4%B8%BA%E4%B8%8E%E5%A4%B1%E6%95%88%E5%9C%BA%E6%99%AF/</url>
    
    <content type="html"><![CDATA[<p><code>@Transactional</code> 注解无疑是我们处理数据库事务最得力的助手。然而，在复杂的业务链路下，仅仅知道“加上注解就能回滚”是远远不够的，我们还需要掌握事务的传播行为以及各种隐蔽的失效场景。</p><h3 id="Transactional-是如何工作的"><a href="#Transactional-是如何工作的" class="headerlink" title="@Transactional 是如何工作的"></a><code>@Transactional</code> 是如何工作的</h3><p>要理解事务为何生效、又为何失效，首先必须明白 <code>@Transactional</code> 是如何工作的。</p><p><code>@Transactional</code> 注解的实现高度依赖于 Spring AOP 的动态代理机制——当一个标记了该注解的方法被调用时，Spring 并不会直接执行这个方法，而是 <strong>生成一个代理对象来包裹原始对象</strong>。</p><blockquote><p>在 Java 中，方法永远是属于某个类的实例（对象）的 —— 你无法单独调用一个 “脱离对象的方法”。调用方法的本质是 “给某个对象发指令，让它执行对应的行为”。</p><p>所以当我们说 “标记 @Transactional 的方法被调用” 时，这个调用动作的载体必然是 <strong>该方法所属的对象</strong>。Spring 要拦截这个方法的执行、添加事务逻辑，就必须先拦截对这个对象的方法调用。</p></blockquote><p>所有对该方法的外部调用，都会先经过这个代理对象。代理对象负责开启事务，随后调用原始对象的实际方法，最后根据方法的执行结果来决定是提交还是回滚事务。</p><p>这个代理对象的核心逻辑是：</p><ol><li>持有原始对象的引用</li><li>所有对原始对象方法的调用，都会先经过代理对象</li><li>代理对象会判断当前调用的方法是否标记了 @Transactional：<ol><li>如果是：先执行事务开启逻辑 → 调用原始对象的该方法 → 执行事务提交 &#x2F; 回滚；</li><li>如果不是：直接转发调用到原始对象的方法，不做额外处理。</li></ol></li></ol><figure class="highlight java"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br><span class="line">17</span><br><span class="line">18</span><br><span class="line">19</span><br><span class="line">20</span><br><span class="line">21</span><br><span class="line">22</span><br><span class="line">23</span><br><span class="line">24</span><br><span class="line">25</span><br><span class="line">26</span><br><span class="line">27</span><br></pre></td><td class="code"><pre><code class="hljs java"><span class="hljs-comment">// Spring生成的代理对象（简化版）</span><br><span class="hljs-keyword">public</span> <span class="hljs-keyword">class</span> <span class="hljs-title class_">AccountServiceProxy</span> <span class="hljs-keyword">extends</span> <span class="hljs-title class_">AccountService</span> &#123;<br>    <span class="hljs-comment">// 持有原始对象</span><br>    <span class="hljs-keyword">private</span> AccountService target;<br><br>    <span class="hljs-meta">@Override</span><br>    <span class="hljs-keyword">public</span> <span class="hljs-keyword">void</span> <span class="hljs-title function_">transferMoney</span><span class="hljs-params">(Long fromId, Long toId, BigDecimal amount)</span> &#123;<br>        <span class="hljs-comment">// 代理逻辑：开启事务（这是@Transactional的核心）</span><br>        System.out.println(<span class="hljs-string">&quot;开启数据库事务&quot;</span>);<br>        <span class="hljs-keyword">try</span> &#123;<br>            <span class="hljs-comment">// 调用原始对象的transferMoney方法</span><br>            target.transferMoney(fromId, toId, amount);<br>            <span class="hljs-comment">// 提交事务</span><br>            System.out.println(<span class="hljs-string">&quot;提交事务&quot;</span>);<br>        &#125; <span class="hljs-keyword">catch</span> (Exception e) &#123;<br>            <span class="hljs-comment">// 回滚事务</span><br>            System.out.println(<span class="hljs-string">&quot;回滚事务&quot;</span>);<br>            <span class="hljs-keyword">throw</span> e;<br>        &#125;<br>    &#125;<br><br>    <span class="hljs-meta">@Override</span><br>    <span class="hljs-keyword">public</span> BigDecimal <span class="hljs-title function_">queryBalance</span><span class="hljs-params">(Long userId)</span> &#123;<br>        <span class="hljs-comment">// 无事务，直接调用原始对象的方法</span><br>        <span class="hljs-keyword">return</span> target.queryBalance(userId);<br>    &#125;<br>&#125;<br></code></pre></td></tr></table></figure><p>记住这个结论：<strong>事务生效的前提是，方法的调用必须经过 Spring 代理对象的拦截</strong>。</p><h3 id="事务传播行为"><a href="#事务传播行为" class="headerlink" title="事务传播行为"></a>事务传播行为</h3><p>接下来稍微叉出去，讲一下另一个东西。</p><p>在复杂的微服务或模块化设计中，经常会出现“事务方法 A 调用事务方法 B”的情况。此时，被调用的方法是应该加入现有的事务，还是另起炉灶？这就是 <strong>事务传播行为（Propagation）</strong> 要解决的问题。</p><p>Spring 在 <code>Propagation</code> 枚举中定义了 7 种行为，我们可以 <strong>按“对当前事务的态度”将其分为三类</strong>：</p><h4 id="融入型"><a href="#融入型" class="headerlink" title="融入型"></a>融入型</h4><ul><li><strong>REQUIRED（默认）</strong>：同生共死，如果当前存在事务，则加入该事务；若没有，则新建一个。它们在物理层面上共享同一个数据库连接。只要调用链中任何一个方法抛出异常，整个事务（内外层）都会被标记为 <code>rollback-only</code>，最终统一回滚。适用于 90% 强调原子性的增删改场景。</li><li><strong>SUPPORTS</strong>：墙头草。有事务就加入，没事务就以非事务方式运行。常用于单纯的查询操作。</li><li><strong>MANDATORY</strong>：寄生虫。必须在事务中运行，如果当前没事务，直接抛出异常。适用于缺乏独立业务语义、强制要求上层开启事务的 DAO 层方法。</li></ul><h4 id="独立型"><a href="#独立型" class="headerlink" title="独立型"></a>独立型</h4><ul><li><p><strong>REQUIRES_NEW</strong>：完全隔离，各玩各的。总是创建一个新的独立事务。如果当前有事务，Spring 会将外层事务的资源解绑并挂起，开启全新的数据库连接。内外层事务完全隔离。内层回滚不影响外层（除非异常抛出未被捕获），外层回滚也不影响内层已提交的数据。适用场景于审计日志入库（业务失败日志也要记录）、序列号生成。</p><ul><li><strong>风险</strong>：高并发下同时持有两个连接，易耗尽连接池；操作同行数据易引发死锁。</li></ul></li><li><p><strong>NOT_SUPPORTED</strong>：暂停事务。强制挂起当前事务，以非事务方式运行。适用于发送邮件&#x2F;短信等耗时且无需事务的操作。</p></li><li><p><strong>NEVER</strong>：事务洁癖。以非事务方式运行，若存在事务则直接抛异常。</p></li></ul><h4 id="嵌套型"><a href="#嵌套型" class="headerlink" title="嵌套型"></a>嵌套型</h4><p><strong>NESTED</strong>：设存档，失败重来。依赖 JDBC 的 <code>Savepoint</code> 技术，与外层共享物理连接。如果有事务，则在嵌套事务中运行；没有则类似 <code>REQUIRED</code>。内层回滚仅回到保存点，不影响外层；但外层回滚会连带内层一起回滚。适用于非核心步骤（如次要数据更新）失败可忽略，但不应拖累主业务的场景。</p><h3 id="盘点-Spring-事务的四类失效场景"><a href="#盘点-Spring-事务的四类失效场景" class="headerlink" title="盘点 Spring 事务的四类失效场景"></a>盘点 Spring 事务的四类失效场景</h3><p>理解了原理和机制，我们来看看实战中那些导致事务失效的元凶。基于 AOP 的底层逻辑，失效原因通常可以归结为以下几类：</p><h4 id="AOP-代理绕过"><a href="#AOP-代理绕过" class="headerlink" title="AOP 代理绕过"></a>AOP 代理绕过</h4><p><strong>1.同类内部自调用</strong>：当类中一个 <strong>无事务的方法 A</strong> 直接 <strong>调用同类中有事务的方法 B</strong>（即 <code>this.methodB()</code>）时，因为 <strong>直接访问的是原始对象，绕过了代理类</strong>，事务彻底失效。</p><p>解决方法：</p><ol><li>将方法拆分到新 Service 类（最优雅）</li><li>使用 <code>@Lazy</code> 注入自身代理对象</li><li>开启 <code>expose-proxy</code> 后使用 <code>AopContext.currentProxy()</code> 调用。</li></ol><p><strong>2.方法访问权限非 <code>public</code></strong>：无论是 JDK 动态代理（只能代理接口）还是 CGLIB（重写父类），Spring AOP <strong>规范</strong> 默认只拦截 <code>public</code> 方法。加在 <code>private</code> 或 <code>protected</code> 方法上的事务注解形同虚设。</p><blockquote><p>CGLIB 确实 <strong>无法</strong> 拦截 private（Java 语言规范：子类不可见父类私有成员），但技术上 CGLIB <strong>完全可以</strong> 拦截 protected 方法（通过 ASM 字节码生成子类重写 protected 方法）。</p><p>所以说 Spring 选择不拦截 protected，是 <strong>框架层面的故意限制</strong>，而非技术不能。</p><p>两个层面来讲，JDK 代理天然处理 public 方法（接口方法默认 public），所以 JDK 代理实际上 <strong>永远接触不到非 public 方法</strong>。为了保证「无论底层用 JDK 还是 CGLIB，AOP 的行为表现完全一致」，Spring 选择统一。</p><p>其次，「为什么你要在 private 方法上加事务？」Private 方法是类内部实现细节，AOP 是 <strong>外部横切关注点</strong>。</p><p>如果允许 <code>@Transactional</code> 作用于 private 方法，等于鼓励「通过外部切面修改对象内部状态」，这彻底摧毁了封装。</p></blockquote><p><strong>3.方法被 <code>final</code> 或 <code>static</code> 修饰</strong>：<code>final</code> 导致 CGLIB 无法重写子类方法；<code>static</code> 方法属于类而非实例，AOP 无法代理。</p><p><strong>4.目标对象未被 Spring 容器管理</strong>：AOP 代理的前提是：目标对象必须是 Spring 容器管理的 Bean。如果类没有通过 <code>@Component</code>&#x2F;<code>@Service</code>&#x2F;<code>@Repository</code>&#x2F;<code>@Controller</code> 等注解声明，也没有通过 XML 配置、<code>@Bean</code> 方法等方式注册到 Spring 容器中，那么 Spring 不会为这个类生成代理对象。此时 <code>@Transactional</code> 注解只是一个 “无意义的标记”，Spring 事务切面根本无法感知到这个类的存在，事务自然完全失效。</p><h4 id="异常处理不当"><a href="#异常处理不当" class="headerlink" title="异常处理不当"></a>异常处理不当</h4><ul><li><strong>异常被 <code>try-catch</code> 吞没</strong>：Spring 默认感知到异常才会回滚。如果你在方法内 <code>catch</code> 了异常却没有重新抛出，Spring 会认为执行成功从而提交事务。</li><li><strong><code>rollbackFor</code> 属性未设置</strong>：默认情况下，事务只对 <code>RuntimeException</code> 和 <code>Error</code> 回滚。如果抛出受检异常（如 <code>IOException</code>），默认会提交事务。</li><li><strong>隐蔽的 <code>UnexpectedRollbackException</code></strong>：在 <code>REQUIRED</code> 模式下，如果内层方法抛出异常，全局事务已被标记为 <code>rollback-only</code>。此时若外层方法 catch 了异常并试图正常提交，Spring 检测到状态不一致，就会抛出此异常。</li></ul><blockquote><ol><li>重新 <code>throw e</code>，或手动调用 <code>TransactionAspectSupport.currentTransactionStatus().setRollbackOnly()</code>。</li><li>养成好习惯，始终指定 <code>@Transactional(rollbackFor = Exception.class)</code>。</li><li>若内层失败不应影响外层，应将内层事务改为 <code>REQUIRES_NEW</code> 或 <code>NESTED</code>。</li></ol></blockquote><h4 id="线程与上下文断裂"><a href="#线程与上下文断裂" class="headerlink" title="线程与上下文断裂"></a>线程与上下文断裂</h4><p><strong>多线程或异步调用</strong>：Spring 事务信息默认通过 <code>ThreadLocal</code> 与当前线程绑定。在事务方法内新开线程或调用 <code>@Async</code> 方法，子线程无法获取父线程的事务上下文，操作将游离于事务之外。</p><p>解决方案: 这是一个复杂的问题，通常需要使用支持事务传播的异步任务执行器（如 <code>TransactionAwareTaskDecorator</code>）或手动在子线程中管理事务。对于大多数场景，建议避免在事务方法内部进行复杂的异步或多线程数据库操作。</p><h4 id="架构与环境配置错误"><a href="#架构与环境配置错误" class="headerlink" title="架构与环境配置错误"></a>架构与环境配置错误</h4><ul><li><strong>数据库引擎不支持</strong>：即使代码完美，如果 MySQL 表使用的是 MyISAM 这种不支持事务的引擎，事务自然无效（需改为 InnoDB）。</li><li><strong>动态数据源切换冲突</strong>：读写分离架构下，若数据源切换的 AOP 切面执行顺序晚于事务切面，事务会先在主库拿到连接，导致后续的读操作也全部落在主库上。必须通过 <code>@Order</code> 确保数据源切换的优先级更高。</li></ul>]]></content>
    
    
    <categories>
      
      <category>Spring框架</category>
      
    </categories>
    
    
    <tags>
      
      <tag>事务管理</tag>
      
      <tag>Spring AOP</tag>
      
      <tag>事务失效</tag>
      
    </tags>
    
  </entry>
  
  
  
  <entry>
    <title>TCP与UDP</title>
    <link href="/2026/02/12/TCP%E4%B8%8EUDP/"/>
    <url>/2026/02/12/TCP%E4%B8%8EUDP/</url>
    
    <content type="html"><![CDATA[<p>想象你正在开发一个实时对战游戏。当玩家按下“开火”键时，数据必须毫秒级到达服务器。如果网络稍微抖动，你希望发生什么？</p><p>A. 游戏卡住，等待 2 秒，直到这个“开火”指令确认送达，然后画面瞬间快进。</p><p>B. 丢弃这个指令，画面保持流畅，玩家可能只是觉得刚才没打中。</p><p>显然后者体验更好。但在另一个场景，当你从银行下载电子账单时，如果丢失了一个数据包，导致金额从 <code>10,000</code> 变成了 <code>1,00</code>，这是绝对不可接受的。</p><p>这就是网络传输设计的核心矛盾 <strong>：绝对的可靠性</strong> 与 <strong>极致的低延迟</strong> 往往不可兼得。互联网协议栈给出的答案是两个截然不同的选择 <strong>：TCP (传输控制协议，Transmission Control Protocol)</strong> 和 <strong>UDP (用户数据报协议，User Datagram Protocol)</strong>。</p><p>TCP 追求数据的绝对完整、有序和可靠交付，不惜牺牲一定的传输效率。因此具有 <strong>可靠性</strong>。面向连接、可靠传输、面向字节流、全双工。UDP 追求最快的传输速度和最低的延迟，对丢包不敏感。无连接、不可靠传输、面向数据报、轻量级。</p><p>UDP 的头部只有固定的 <strong>8 字节</strong>，轻量到了极致。它不打算在这个层面做任何控制，只是为了把数据“分发”给应用程序。</p><ul><li><strong>源&#x2F;目的端口 (16 位)</strong>：就像信封上的“收件人房间号”。IP 地址解决了“寄到哪栋楼”，端口解决了“交给哪个前台（进程）”。</li><li><strong>长度 (16 位)</strong>：头部+数据的总长度。</li><li><strong>校验和 (16 位)</strong>：唯一的安全措施。如果数据在传输中比特翻转了（0 变 1），接收方算出的校验和会对不上，直接丢弃该包。可选。</li></ul><p>因为简单，UDP 没有拥塞控制。这意味着当网络拥堵时，UDP 依然会全速发送，可能加剧网络瘫痪。但这正是视频会议需要的——与其重传 3 秒前的画面，不如直接丢弃，播放最新的帧。</p><p>为了实现可靠性，TCP 头部承载了沉重的控制信息。最小 <strong>20 字节</strong>，最大 <strong>60 字节</strong>（取决于选项 Options）。事情就变得复杂了。我们需要解决两个核心问题 <strong>：丢包</strong> 和 <strong>乱序</strong>。</p><p>为此，TCP 引入了：</p><ul><li>序列号 (<strong>Seq</strong> uence, 32 位)：数据的身份证。解决 <strong>乱序</strong> 问题。</li><li>确认号 (<strong>Ack</strong> nowledgment&#x2F;<strong>ack</strong>, 32 位)：接收方的回执，指明期望收到的下一个字节序号。解决 <strong>丢包</strong> 问题。</li></ul><p>发送方：我发送了第 $100$ 到 $200$ 字节。</p><p>接收方：我收到了第 $200$ 字节之前的所有数据，下次请发 $201$。</p><p><strong>标志位</strong>：控制连接状态的红绿灯。</p><ul><li><strong>SYN</strong>：发起连接。</li><li><strong>ACK</strong>：确认收到。</li><li><strong>FIN</strong>：结束连接。</li><li><strong>RST</strong>：重置连接（强制挂断，通常发生在异常时）。</li><li><strong>PSH</strong>：推数据（催促接收方尽快交给应用层，别在缓存里攒着）。</li></ul><blockquote><p>ack&#x2F;Ack 是确认号，ACK 是标志位</p></blockquote><p>窗口大小 (16 位)：流量控制的核心。</p><p>校验和 (Checksum)：强制校验。</p><h3 id="连接的建立：三次握手"><a href="#连接的建立：三次握手" class="headerlink" title="连接的建立：三次握手"></a>连接的建立：三次握手</h3><p>TCP 三次握手有两个核心目的：一是 <strong>同步双方初始序列号（ISN，Initial Sequence Number）</strong>，并 <strong>可靠确认双方发送与接收能力均正常</strong>；二是 <strong>防止网络中滞留的历史连接报文导致错误建立连接</strong>。</p><ul><li><strong>Step 1 (SYN)</strong>：客户端发送 <code>SYN=1</code>，生成随机序列号 <code>seq=x</code>。</li><li><strong>Step 2 (SYN+ACK)</strong>：服务端收到后，回复 <code>SYN=1</code> (我也要连你) + <code>ACK=1</code> (确认收到你的)。服务端生成自己的序列号 <code>seq=y</code>，确认号 <code>ack=x+1</code>。</li><li><strong>Step 3 (ACK)</strong>：客户端收到后，检查 <code>ack</code> 是否正确，回复 <code>ACK=1</code>，确认号 <code>ack=y+1</code>。</li></ul><blockquote><p>为什么初始序列号 (ISN) 必须是随机的？</p><p>为了防止黑客伪造 TCP 包（TCP 序列号预测攻击），也为了防止网络中滞留的旧数据包干扰新连接。</p></blockquote><pre><code class=" mermaid">sequenceDiagram    participant C as 客户端 (Client)    participant S as 服务器 (Server)    Note over C,S: 状态: CLOSED    C-&gt;&gt;S: SYN=1, Seq=x    Note over C: SYN_SENT    Note over S: LISTEN -&gt; SYN_RCVD    S-&gt;&gt;C: SYN=1, ACK=1, Seq=y, Ack=x+1    Note over C: ESTABLISHED    C-&gt;&gt;S: ACK=1, Seq=x+1, Ack=y+1    Note over S: ESTABLISHED    Note right of C: 连接建立，可以传输数据</code></pre><h4 id="为什么是三次握手？"><a href="#为什么是三次握手？" class="headerlink" title="为什么是三次握手？"></a>为什么是三次握手？</h4><p>为什么不是两次？</p><p>主要目的是 <strong>防止历史连接的初始化</strong>。</p><p>假设客户端发出的第一个 SYN 包在网络中滞留了很久，客户端超时重发并建立了新连接。数据传完断开后，那个滞留的“旧 SYN”终于到了服务器。</p><ul><li>如果是两次握手：服务器收到旧 SYN 以为是新连接，直接分配资源，导致服务器空挂一个连接。</li><li>如果是三次握手：服务器收到旧 SYN，回复 SYN+ACK。客户端收到后，发现 <code>Ack</code> 对应的是旧请求，直接发送 <code>RST</code> 报文终止，避免资源浪费。</li></ul><h3 id="断开连接：四次挥手"><a href="#断开连接：四次挥手" class="headerlink" title="断开连接：四次挥手"></a>断开连接：四次挥手</h3><p>TCP 是全双工的（双向独立通道），因此关闭需要两个方向分别操作。</p><ul><li>管道 A：客户端 $\rightarrow$ 服务器</li><li>管道 B：服务器 $\rightarrow$ 客户端</li></ul><p><strong>主动关闭</strong>：只是切断了 <strong>管道 A</strong>。<strong>管道 B</strong> 还通着，因为 <strong>被动方还要发数据</strong>。</p><pre><code class=" mermaid">sequenceDiagram    participant C as 客户端 (主动关闭)    participant S as 服务器 (被动关闭)    Note over C,S: 场景：客户端请求下载大文档    C-&gt;&gt;S: 发送请求 &quot;GET /movie.mp4&quot;    C-&gt;&gt;S: FIN (我请求发完了，我不说话了)    Note over C: 状态: FIN_WAIT_1    S-&gt;&gt;C: ACK (知道了，你闭嘴吧)    Note over C: 状态: FIN_WAIT_2 (耳朵还竖着)        Note right of S: 【关键阶段】&lt;br/&gt;服务器开始疯狂发送 1GB 的电影数据    S-&gt;&gt;C: 数据包 1 (电影开头...)    C--&gt;&gt;S: ACK (收到了)    S-&gt;&gt;C: 数据包 2 (电影中段...)    C--&gt;&gt;S: ACK (收到了)    S-&gt;&gt;C: ... 数据包 N ...    C--&gt;&gt;S: ACK (收到了)    Note right of S: 电影发完了    S-&gt;&gt;C: FIN (我也说完了)    Note over C: 状态: TIME_WAIT    C-&gt;&gt;S: ACK (好的，再见)    Note over S: CLOSED</code></pre><ul><li><strong>Step 1 (FIN)</strong>：主动方发 <code>FIN</code>。主动方：我发完了，申请关闭。</li><li><strong>Step 2 (ACK)</strong>：被动方回 <code>ACK</code>。此时连接处于 <strong>半关闭</strong> 状态。此时被动方可能还有剩余数据要发送。</li><li><strong>Step 3 (FIN)</strong>：被动方数据传完后，也发 <code>FIN</code>。</li><li><strong>Step 4 (ACK)</strong>：主动方回 <code>ACK</code>，并进入 <code>TIME_WAIT</code>（等待 2MSL）。服务端收到后直接 <code>CLOSED</code>。</li></ul><pre><code class=" mermaid">sequenceDiagram    participant A as 主动方 (Active)    participant P as 被动方 (Passive)    Note over A: ESTABLISHED    A-&gt;&gt;P: FIN=1, Seq=u    Note over A: FIN_WAIT_1    Note over P: CLOSE_WAIT    P-&gt;&gt;A: ACK=1, Ack=u+1    Note over A: FIN_WAIT_2    Note right of P: 此时 P 仍可发送剩余数据    P-&gt;&gt;A: FIN=1, Seq=w, Ack=u+1    Note over P: LAST_ACK    A-&gt;&gt;P: ACK=1, Ack=w+1    Note over A: TIME_WAIT (2MSL)    Note over P: CLOSED    Note over A: CLOSED</code></pre><p>为什么 <strong>主动关闭方要进入 TIME_WAIT 状态等待 2MSL</strong>？</p><p>MSL（Maximum Segment Lifetime）：报文最大生存时间</p><p>如果 A 回复完最后一个 ACK 直接跑路（CLOSED）：</p><ol><li><strong>可靠性崩塌</strong>：如果最后一个 ACK 丢了，B 会重发 FIN。如果 A 已经关机或释放了端口，B 就会收到 RST 错误，导致传输不完美的结束。</li><li><strong>幽灵数据</strong>：IP 协议允许数据包在网络中“迷路”一段时间。如果没有 TIME_WAIT，A 立即在同一个端口启动了新连接。此时，那个迷路的老数据包突然到达，会被新连接误接收，导致数据错乱。</li></ol><blockquote><p>在高并发短连接场景（如 Web 服务器）下，大量的 <code>TIME_WAIT</code> 会占用服务器端口资源（最多 65535 个）。</p><p><strong>优化方案</strong>：开启 <code>tcp_tw_reuse</code>（允许安全重用），但尽量不要开启 <code>tcp_tw_recycle</code>（在 NAT 环境下会导致连接失败）。</p></blockquote><p>数据流与“粘包”问题</p><p>TCP 是 <strong>面向字节流 (Byte Stream)</strong> 的。这意味着 TCP 不把数据看作一个个“包”，而是一条连绵不断的水流。</p><p><strong>实现痛点</strong>：你在客户端 <code>send(&quot;Hello&quot;)</code> 两次。服务端可能收到 <code>recv(&quot;HelloHello&quot;)</code>，也可能收到 <code>recv(&quot;He&quot;)</code> 和 <code>recv(&quot;lloHello&quot;)</code>。这在口语中被称为“粘包”，实质上是 TCP 根本就没有“包”的概念。</p><p><strong>代码实现</strong>：应用层协议必须自己定义边界。最通用的做法是 <strong>Header + Body</strong> 模式，Header 中包含 Body 的长度。</p><figure class="highlight python"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br><span class="line">17</span><br><span class="line">18</span><br><span class="line">19</span><br><span class="line">20</span><br><span class="line">21</span><br><span class="line">22</span><br></pre></td><td class="code"><pre><code class="hljs python"><span class="hljs-keyword">def</span> <span class="hljs-title function_">read_message</span>(<span class="hljs-params">socket</span>):<br>    <span class="hljs-comment"># 1. 先读取固定长度的头部 (假设头部 4 字节存长度)</span><br>    header_data = read_exactly(socket, <span class="hljs-number">4</span>)<br>    <span class="hljs-keyword">if</span> <span class="hljs-keyword">not</span> header_data:<br>        <span class="hljs-keyword">return</span> <span class="hljs-literal">None</span><br>    <br>    <span class="hljs-comment"># 2. 解析出消息体长度</span><br>    body_length = decode_int(header_data)    <br>    <span class="hljs-comment"># 3. 根据长度读取完整的消息体</span><br>    body_data = read_exactly(socket, body_length)<br>    <br>    <span class="hljs-keyword">return</span> body_data<br><br><span class="hljs-keyword">def</span> <span class="hljs-title function_">read_exactly</span>(<span class="hljs-params">socket, n</span>):<br>    <span class="hljs-comment"># 循环读取，直到读够 n 个字节</span><br>    data = <span class="hljs-string">b&#x27;&#x27;</span><br>    <span class="hljs-keyword">while</span> <span class="hljs-built_in">len</span>(data) &lt; n:<br>        packet = socket.recv(n - <span class="hljs-built_in">len</span>(data))<br>        <span class="hljs-keyword">if</span> <span class="hljs-keyword">not</span> packet:<br>            <span class="hljs-keyword">return</span> <span class="hljs-literal">None</span><br>        data += packet<br>    <span class="hljs-keyword">return</span> data<br></code></pre></td></tr></table></figure><h3 id="流量控制-vs-拥塞控制"><a href="#流量控制-vs-拥塞控制" class="headerlink" title="流量控制 vs. 拥塞控制"></a>流量控制 vs. 拥塞控制</h3><p>这个两个完全不同的问题：<strong>接收方吃不消</strong> vs <strong>网络堵死了</strong>。</p><h4 id="流量控制：保护接收方"><a href="#流量控制：保护接收方" class="headerlink" title="流量控制：保护接收方"></a>流量控制：保护接收方</h4><p>机制：<strong>滑动窗口</strong> (Sliding Window)。“点对点”的协商</p><p><strong>场景</strong>：发送方是高性能服务器，接收方是一台老旧手机。如果服务器全速发送，手机的处理速度跟不上，缓冲区爆满，数据就会溢出丢失。</p><p><strong>原理</strong>：</p><ul><li>接收方在 TCP 头部的 <code>Window</code> 字段中写入自己当前的缓冲区剩余大小（例如 4KB）。</li><li>发送方看到后，就会限制自己，最多只发 4KB 数据，然后暂停等待新的 ACK。</li><li>如果接收方处理不过来，<code>Window</code> 可能会变成 0（<strong>零窗口</strong>），此时发送方完全停止发送，直到接收方发送窗口更新通知。</li></ul><h4 id="拥塞控制：保护网络环境"><a href="#拥塞控制：保护网络环境" class="headerlink" title="拥塞控制：保护网络环境"></a>拥塞控制：保护网络环境</h4><p>机制：<strong>拥塞窗口</strong> (<strong>cwnd</strong>, Congestion Window)。发送方通过试探猜测网络的承载能力。</p><p>发送方维护一个状态变量 <code>cwnd</code>，表示在未收到 ACK 之前，我敢往网络里扔多少数据。</p><p>拥塞控制包含四个核心算法，构成了一个闭环的控制系统：</p><h5 id="慢启动——-试探"><a href="#慢启动——-试探" class="headerlink" title="慢启动—— 试探"></a>慢启动—— 试探</h5><ul><li><strong>设计思想</strong>：刚创建连接时，不知道网络深浅，如果上来就全速发送，可能会瞬间把网络堵死。</li><li><strong>机制</strong>：<ul><li>连接刚创建，<code>cwnd = 1</code>。</li><li>每收到一个 ACK，<code>cwnd</code> 加倍（1 -&gt; 2 -&gt; 4 -&gt; 8…）。</li><li><strong>呈指数级增长</strong>。这叫“慢”启动，但实际上加速极快，目的是在极短时间内找到网络的带宽上限。</li></ul></li></ul><h5 id="拥塞避免——-巡航"><a href="#拥塞避免——-巡航" class="headerlink" title="拥塞避免—— 巡航"></a>拥塞避免—— 巡航</h5><ul><li><strong>设计思想</strong>：当 <code>cwnd</code> 增长到一个阈值（ssthresh）时，说明快接近极限了，不能再翻倍了，要小心翼翼。</li><li><strong>机制</strong>：<ul><li>每经过一个往返时间 (RTT)，<code>cwnd</code> 只增加 1。</li><li><strong>线性增长</strong>（加法增大），以此来缓慢探测网络的最高吞吐量。</li></ul></li></ul><h5 id="拥塞发生——-刹车"><a href="#拥塞发生——-刹车" class="headerlink" title="拥塞发生—— 刹车"></a>拥塞发生—— 刹车</h5><p>当出现丢包（超时或收到重复 ACK）时，TCP 认为网络拥堵了，必须踩刹车。</p><p>当出现 <strong>超时重传（RTO Timeout）</strong>，这是最严重的情况（连 ACK 都回不来了）。TCP 会判定网络极度拥堵。<code>cwnd</code> 直接重置为 1，重新进入“慢启动”。这是“一脚刹车踩死”，传输速度瞬间掉底。</p><h5 id="快重传与快恢复——-点刹"><a href="#快重传与快恢复——-点刹" class="headerlink" title="快重传与快恢复—— 点刹"></a>快重传与快恢复—— 点刹</h5><ul><li><strong>设计思想</strong>：有时候丢包不是因为拥堵，而是因为偶尔的链路错误。如果只丢了一个包，接收方会连续回复 3 个相同的 ACK（重复确认）。</li><li><strong>机制</strong>：<ul><li><strong>快重传</strong>：发送方一旦收到 <strong>3 个重复的 ACK</strong>，就知道“有个特定的包丢了”，立刻重传该包，而 <strong>不等待超时定时器</strong>。</li><li><strong>快恢复</strong>：既然还能收到重复 ACK，说明网络没断，不需要重置为 1。于是将 <code>cwnd</code> 减半（乘法减小），然后进入“拥塞避免”阶段。</li></ul></li></ul><p>TCP 的发送速度 &#x3D; <code>min(接收方窗口, 拥塞窗口)</code>。 它永远在 <strong>“试探 -&gt; 增长 -&gt; 丢包 -&gt; 减速 -&gt; 再试探”</strong> 的循环中寻找平衡点。这就是为什么下载大文档时，速度曲线总是呈锯齿状波动的原因。</p><h3 id="TCP-的局限与未来"><a href="#TCP-的局限与未来" class="headerlink" title="TCP 的局限与未来"></a>TCP 的局限与未来</h3><p>TCP 设计于 70 年代，它假设“网络丢包是偶然的”且“按序处理是必须的”。但在现代网络中，这带来了 <strong>队头阻塞 (Head-of-Line Blocking)</strong> 问题。</p><p>如果 TCP 窗口内的一个包丢失，即使后续的包都到了，内核也不能把数据交给应用层，必须等那个丢失的包重传回来。这在网络不稳定的移动端会导致巨大的延迟波动。</p><p>为了解决这个问题，Google 推出了 QUIC(HTTP&#x2F;3) 协议。有趣的是，<strong>QUIC 底层使用的是 UDP</strong>。它在 UDP 之上重新实现了可靠传输、拥塞控制，但抛弃了 TCP 的强序依赖，实现了真正的多路复用。</p>]]></content>
    
    
    <categories>
      
      <category>计算机网络</category>
      
    </categories>
    
    
    <tags>
      
      <tag>TCP</tag>
      
      <tag>UDP</tag>
      
      <tag>网络协议</tag>
      
    </tags>
    
  </entry>
  
  
  
  <entry>
    <title>MySQL并发安全_锁与MVCC机制</title>
    <link href="/2026/01/31/MySQL%E5%B9%B6%E5%8F%91%E5%AE%89%E5%85%A8_%E9%94%81%E4%B8%8EMVCC%E6%9C%BA%E5%88%B6/"/>
    <url>/2026/01/31/MySQL%E5%B9%B6%E5%8F%91%E5%AE%89%E5%85%A8_%E9%94%81%E4%B8%8EMVCC%E6%9C%BA%E5%88%B6/</url>
    
    <content type="html"><![CDATA[<p>从计算机科学的视角来看，<strong>数据库管理系统本质上是一个维护 ACID 特性的状态机</strong>。在单线程模型下，状态转换是确定的。然而，一旦引入并发，系统便陷入了混乱。</p><p>InnoDB 存储引擎的并发模块主要目的就是 <strong>在并发吞吐量与数据一致性之间寻找平衡</strong>。这是一个零和博弈——<strong>隔离性越高，并发度就越低</strong>。</p><h2 id="并发带来的问题"><a href="#并发带来的问题" class="headerlink" title="并发带来的问题"></a>并发带来的问题</h2><p>当多个事务同时操作同一份数据时，如果没有合适的隔离机制，可能会出现以下三种一致性问题：</p><ul><li><strong>脏读</strong>：一个事务 A 读取到了另一个事务 B <strong>尚未提交</strong> 的数据。如果这个事务 B 最终回滚了，那么事务 A 基于读取到的数据的操作就会导致错误的结果。</li><li><strong>不可重复读</strong>：同一个事务内，多次读取同一个数据，得到的结果却不一致（可能是其它事务修改了数据）。</li><li><strong>幻读（Phantom Read）</strong>：同一个事务内，多次执行同一个查询条件的 SQL，结果集的行数不一致（可能是其它事务插入&#x2F;删除了数据）。</li></ul><p>为了解决这些问题，SQL 标准定义了四种隔离级别（RU, RC, RR, Serializable）。</p><p>这四种事务隔离级别，级别越高，数据一致性越好，但并发性能越低。</p><ol><li><strong>READ UNCOMMITTED（读未提交）</strong>：允许读取其他事务未提交的数据。极少使用，仅适用于对数据一致性要求极低、追求极致并发性能的场景（如 <strong>日志统计</strong>）。脏读、不可重复读、幻读都可能发生。</li><li><strong>READ COMMITTED（读已提交）</strong>：只能读取其他事务已提交的数据，无法读取未提交的数据。大多数应用的常用级别（<strong>Oracle、SQL Server 默认隔离级别</strong>），平衡了一致性和性能。避免了脏读。但仍可能发生不可重复读和幻读。</li><li><strong>REPEATABLE READ（可重复读）</strong>：<strong>MySQL 默认隔离级别</strong>。同一个事务内多次读取同一数据，结果始终一致（即使其他事务修改并提交了该数据）。避免了脏读、不可重复读。理论上仍可能出现幻读，但 MySQL 的 InnoDB 存储引擎通过 <strong>间隙锁（Next-Key Lock）</strong> 解决了幻读问题，所以 <strong>在 InnoDB 中该级别无幻读</strong>。</li><li><strong>SERIALIZABLE（串行化）</strong>：所有事务串行执行。避免了所有并发问题（脏读、不可重复读、幻读）。并发性能极低（相当于单线程），容易出现事务超时、死锁。极少使用，仅适用于对数据一致性要求极高、并发量极低的场景（如 <strong>金融核心交易</strong>）。</li></ol><p>但 <strong>标准只是规范</strong>，<strong>InnoDB 是如何落地的</strong>？</p><p>核心依赖两大基石：<strong>MVCC（多版本并发控制）</strong> 和 <strong>锁机制</strong>。</p><p>简单来说：<strong>MVCC 解决了“读-写”冲突（无锁并发），而锁解决了“写-写”冲突。</strong></p><h2 id="MVCC"><a href="#MVCC" class="headerlink" title="MVCC"></a>MVCC</h2><p>在传统锁定协议中，读操作会阻塞写操作。MVCC 引入了“<strong>快照读</strong>”的概念，通过保留数据的历史版本，实现 <strong>读写不阻塞</strong>。</p><h3 id="Undo-Log-版本链"><a href="#Undo-Log-版本链" class="headerlink" title="Undo Log 版本链"></a>Undo Log 版本链</h3><p>InnoDB 表中的每一行记录，除了用户数据外，还包含三个隐藏列：</p><ul><li><strong>DB_TRX_ID</strong>：最后一次修改该行的 <strong>事务 ID</strong>。</li><li><strong>DB_ROLL_PTR</strong>：回滚指针，指向 Undo Log 中该行的 <strong>上一个版本</strong>。</li><li><strong>DB_ROW_ID</strong>：隐藏主键（若无显式主键时生成）。</li></ul><p>每当数据被修改，旧版本会被“搬”到 Undo Log 中。<code>DB_ROLL_PTR</code> 将这些历史版本串联成一个单向链表。</p><blockquote><p><strong>Undo Log 会无限膨胀吗？</strong></p><p>不会。InnoDB 有后台 <strong>Purge 线程</strong>。当系统中最老的事务都不再需要某个版本（即没有 Read View 引用它）时，Purge 线程会物理删除这些废弃的 Undo Log，以此回收空间。</p></blockquote><h3 id="Read-View（一致性视图）"><a href="#Read-View（一致性视图）" class="headerlink" title="Read View（一致性视图）"></a>Read View（一致性视图）</h3><p>Read View 是事务进行快照读时生成的“可见性判据”。它记录了 <strong>生成时刻</strong> 系统中所有 <strong>活跃（未提交）</strong> 的事务 ID。</p><p>核心字段：</p><ul><li><code>m_ids</code>：活跃事务 ID 列表。</li><li><code>min_trx_id</code>：<code>m_ids</code> 中的最小值（低水位）。</li><li><code>max_trx_id</code>：系统当前最大事务 ID + 1（高水位）。</li><li><code>creator_trx_id</code>：当前事务自己的 ID。</li></ul><p><strong>RC 级别</strong> 下每条查询语句执行前都会重新创建一个 Read View，确保读取到的是最新的已提交数据（语句级快照）。<strong>RR 级别</strong> 下仅在事务启动后的第一次查询时创建 Read View，整个事务期间复用，从而实现可重复读（事务级快照）。</p><h3 id="可见性算法"><a href="#可见性算法" class="headerlink" title="可见性算法"></a>可见性算法</h3><p>当事务读取某行数据时，会拿该行当前的 <code>DB_TRX_ID</code> 与 Read View 进行比对：</p><ol><li><strong>落在低水位之前</strong> (<code>TRX_ID &lt; min</code>)：说明修改该行的事务早已提交，<strong>可见</strong>。</li><li><strong>落在高水位之后</strong> (<code>TRX_ID &gt;= max</code>)：说明是未来的事务修改的，<strong>不可见</strong>。</li><li><strong>落在水位之间</strong>：<ul><li>若 <code>TRX_ID</code> 在 <code>m_ids</code> 列表中：说明事务还没提交，<strong>不可见</strong>（除非是自己改的）。</li><li>若不在列表里：说明事务已提交，<strong>可见</strong>。</li></ul></li></ol><p><strong>如果当前版本不可见，就顺着 Undo Log 链表找上一个版本，直到找到可见版本为止。</strong></p><p>一句话：只能看到比我老的事务提交的数据和我自己修改的数据。看不到比我新的事务。</p><h2 id="锁机制"><a href="#锁机制" class="headerlink" title="锁机制"></a>锁机制</h2><p>MVCC 搞定了读，但写入时必须加锁。InnoDB 的锁设计非常精妙。</p><h3 id="锁的分类"><a href="#锁的分类" class="headerlink" title="锁的分类"></a>锁的分类</h3><p>MySQL 中的锁可以从 <strong>粒度、兼容性、使用方式、算法</strong> 等多个维度分类：</p><p>按锁的作用（读&#x2F;写）和互斥规则分类：</p><ul><li>共享锁（S 锁）：读锁，允许事务读取数据，多个事务可同时持有同一资源的 S 锁。S 锁与 X 锁互斥（读时不能写，写时不能读）</li><li>排他锁（X 锁）：写锁，允许事务修改&#x2F;删除&#x2F;插入数据。同一资源只能有一个 X 锁；X 锁与任何锁（S&#x2F;X）都互斥</li></ul><p>加锁方式</p><ul><li>隐式锁：InnoDB 自动加锁&#x2F;释放，无需用户干预。  INSERT&#x2F;UPDATE&#x2F;DELETE 自动加 X 锁、普通 SELECT 不加锁。</li><li>显式锁：用户通过 SQL 语句手动加锁，需手动解锁（或事务结束自动解锁）。  <code>SELECT ... FOR UPDATE</code>、<code>LOCK TABLES</code>。</li></ul><p>锁的状态</p><p>意向锁是 InnoDB 为了 <strong>快速判断表级锁和行级锁冲突</strong> 而设计的“表级标记锁”，用户无需手动操作，由数据库自动管理：</p><ul><li><strong>意向共享锁（IS）</strong>：事务准备给某行加 S 锁，先给表加 IS 锁</li><li><strong>意向排他锁（IX）</strong>：事务准备给某行加 X 锁，先给表加 IX 锁</li></ul><p>主要作用是 <strong>避免表锁和行锁的冲突检测耗时</strong>，例如事务 A 给某行加了行锁（隐含表的 IX 锁），事务 B 申请表的 S 锁时，只需检测表的 IX 锁，无需遍历所有行锁，直接判定冲突。</p><p>粒度</p><p><strong>表锁</strong>：加锁时直接锁定整张表，无需遍历行，加锁&#x2F;解锁速度快。InnoDB 中表锁仅在特殊场景触发，比如全表扫描无索引、<code>ALTER TABLE</code> 等 DDL 操作。</p><figure class="highlight plaintext"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br></pre></td><td class="code"><pre><code class="hljs mysql">-- 手动加读表锁（其他事务可读，不可写）<br>LOCK TABLES t_user READ;<br>-- 手动加写表锁（其他事务不可读、不可写）<br>LOCK TABLES t_user WRITE;<br>-- 解锁<br>UNLOCK TABLES;<br></code></pre></td></tr></table></figure><p>InnoDB 默认行锁，但以下场景会触发表锁，进而引发 DML 问题：</p><ul><li>无索引的 DML 操作，行锁退化为表锁：<strong>InnoDB 的行锁基于索引实现</strong>，若 DML 操作的 <code>WHERE</code> 条件无索引（或使用失效索引），InnoDB 会扫描全表并给所有行加行锁，等价于表锁。</li><li>DDL 操作触发表锁，阻塞所有 DML：InnoDB 执行 <code>ALTER TABLE/DROP TABLE</code> 等 DDL 操作时，会强制加表级排他锁，阻塞所有 DML 操作，且 DDL 操作通常耗时较长。MySQL 5.6+ 引入了 <code>Online DDL</code> 优化，部分 DDL 操作（如加列）不会完全阻塞 DML，但核心 DDL（如改主键、删列）仍会触发表锁。</li><li>显式加表锁导致的 DML 阻塞：用户手动执行 <code>LOCK TABLES</code> 加表锁后，所有 DML 操作都会受表锁规则限制，行锁失效。</li></ul><p>内存结构的黑科技：位图与页级锁</p><p>如果要锁住全表 100 万行数据，内存会爆吗？<strong>不会</strong>。</p><p>InnoDB 采用 <strong>页级锁管理 + 位图 (Bitmap)</strong> 的稀疏结构：</p><ul><li>锁结构体 (<code>lock_rec_t</code>) 是挂在 <strong>Page (16KB)</strong> 上的，不是挂在 Row 上的。</li><li>结构体内部用一个 Bitmap 标记该页中哪几行被锁了。</li></ul><p><strong>结论</strong>：无论你锁住一行还是锁住该页的一千行，内存开销是恒定的。这是“空间换时间”的经典反例——通过数据结构优化实现了双赢。</p><p><strong>结论</strong>：无论你锁住一行还是锁住该页的一千行，内存开销是恒定的。这是工程设计中“空间换时间”的经典反例——这里通过数据结构的优化实现了空间和时间的双赢。</p><p><strong>行锁</strong>：<strong>InnoDB 通过索引实现行锁</strong>（<strong>无索引时会退化为表锁</strong>），仅锁定操作的行。</p><figure class="highlight sql"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br></pre></td><td class="code"><pre><code class="hljs sql"><span class="hljs-comment">-- 手动加共享锁（读锁）</span><br><span class="hljs-keyword">SELECT</span> <span class="hljs-operator">*</span> <span class="hljs-keyword">FROM</span> t_user <span class="hljs-keyword">WHERE</span> id<span class="hljs-operator">=</span><span class="hljs-number">1</span> LOCK <span class="hljs-keyword">IN</span> SHARE MODE;<br><span class="hljs-comment">-- 手动加排他锁（写锁）</span><br><span class="hljs-keyword">SELECT</span> <span class="hljs-operator">*</span> <span class="hljs-keyword">FROM</span> t_user <span class="hljs-keyword">WHERE</span> id<span class="hljs-operator">=</span><span class="hljs-number">1</span> <span class="hljs-keyword">FOR</span> <span class="hljs-keyword">UPDATE</span>;<br><span class="hljs-comment">-- DML 操作（INSERT/UPDATE/DELETE）会自动加排他锁，提交/回滚后释放</span><br><span class="hljs-keyword">UPDATE</span> t_user <span class="hljs-keyword">SET</span> name<span class="hljs-operator">=</span><span class="hljs-string">&#x27;李四&#x27;</span> <span class="hljs-keyword">WHERE</span> id<span class="hljs-operator">=</span><span class="hljs-number">1</span>;<br></code></pre></td></tr></table></figure><p><strong>间隙锁</strong>：属于 InnoDB 特有的锁，<strong>锁定“索引记录之间的间隙”</strong>，不锁定具体行。仅在 <code>RR</code> 及以上隔离级别生效。目的是防止其他事务在间隙中插入数据（防止幻读）。</p><p><strong>Next-Key Lock</strong>：临键锁，<strong>RR 级别下 InnoDB 的默认加锁单位</strong>。执行 <code>UPDATE</code>、<code>DELETE</code>、<code>SELECT ... FOR UPDATE</code> 时默认触发。组合了“行锁 + 间隙锁”。锁定范围为：<code>(上一个索引值, 当前索引值]</code>。例如：ID 为 10、20 的行，Next-Key Lock 会锁定 <code>(10,20]</code>（包含 20 行 + 10~20 间隙）。是 InnoDB 解决幻读的核心机制。</p><p>Next-Key Lock 并不是一种全新的锁，而是行锁与间隙锁的组合。</p><h3 id="不同隔离级别的实现"><a href="#不同隔离级别的实现" class="headerlink" title="不同隔离级别的实现"></a>不同隔离级别的实现</h3><p>MySQL 中，SELECT，也就是读操作，其实分为两种：</p><p>普通的 <code>SELECT</code>——<strong>一致性读，又称为快照读</strong>。就是使用快照信息显示基于某个时间点的查询结果，而不考虑与此同时运行的其他事务所执行的更改。在数据库的 <strong>RC 这种隔离级别中，还支持 “半一致读”</strong> ，一条 update 语句，如果 where 条件匹配到的记录已经加锁，那么 InnoDB 会返回记录最近提交的版本，由 MySQL 上层判断此是否需要真的加锁。</p><p><code>INSERT</code>、<code>UPDATE</code>、<code>DELETE</code>，涉及数据修改时——<strong>当前读</strong>，必须读取最新的、已提交的数据。</p><p><strong>读未提交</strong>：<strong>完全不做隔离控制</strong>，读操作直接读取数据的“最新版本”，写操作仅加基础行锁（不影响读）。</p><p><strong>读已提交</strong>：通过 <strong>MVCC</strong> 实现 <strong>“快照读”</strong>，避免脏读；但同一事务内多次读会读取不同的已提交版本，导致不可重复读。</p><ol><li><strong>读操作（快照读）</strong>：每次执行读 SQL 时，生成一个 <strong>当前时间点的快照</strong>（基于事务 ID），仅读取该快照之前已提交的事务修改的数据版本；</li><li><strong>读操作（当前读）</strong>：对于 <code>select ... for update</code>&#x2F;<code>select ... lock in share mode</code> 这类“当前读”（需要加读锁的读），会加 <strong>共享行锁（S 锁）</strong>，但锁仅在当前语句执行完释放（而非事务结束）。</li><li><strong>写操作</strong>：对修改&#x2F;删除的行加排他行锁（X 锁），锁在事务提交&#x2F;回滚时释放；写操作会阻塞其他写操作，但不阻塞快照读。</li></ol><p>该级别下，MVCC 的“快照”是 <strong>语句级</strong> 的（每次读语句生成新快照），而非事务级；</p><p>仅解决脏读，仍存在不可重复读（因为快照随语句刷新）和幻读（无间隙锁，允许插入新数据）。</p><p><strong>可重复读</strong>：整个事务内所有读操作共用一个 <strong>事务启动时的快照</strong>，确保多次读结果一致；结合间隙锁解决幻读，是 InnoDB 最核心的隔离级别。</p><ol><li><strong>读操作（快照读）</strong>：事务启动时生成一个 <strong>全局快照</strong>（基于事务启动时的最大已提交事务 ID），整个事务内所有读操作都使用这个快照，不再刷新；</li><li><strong>读操作（当前读）</strong>：对于 <code>select ... for update</code> 等当前读，InnoDB 会加 <strong>Next-Key Lock（行锁 + 间隙锁）</strong>：</li><li><strong>写操作</strong>：对修改&#x2F;删除的行加排他行锁（X 锁），并结合间隙锁锁定周边间隙；锁仅在事务提交&#x2F;回滚时释放，确保写操作的互斥性。</li></ol><p>MVCC 的“快照”是 <strong>事务级</strong> 的（整个事务共用一个快照），而非语句级；</p><p>通过 Next-Key Lock 解决了幻读（这是 MySQL 对标准 SQL 隔离级别的优化，标准中 REPEATABLE READ 仍有幻读）；</p><p>写操作会检测“幻读风险”，如果其他事务在间隙插入了数据，当前事务的写操作会失败并回滚，确保一致性。</p><p><strong>串行化</strong>：强制所有事务 <strong>串行执行</strong>，完全禁止并发操作，通过加表级锁&#x2F;严格的行锁+间隙锁实现，彻底解决所有并发问题。</p><p>即使是普通的 <code>select</code>（快照读），也会被强制转为 <strong>当前读</strong>，并对读取的行&#x2F;间隙加 <strong>S 锁</strong>；S 锁会持续到事务结束，导致其他事务的写操作必须等待当前事务完成。对修改&#x2F;删除&#x2F;插入的行加排他行锁（X 锁），并对所有相关间隙加间隙锁；排他锁会阻塞所有其他事务的读&#x2F;写操作，直到当前事务提交&#x2F;回滚。</p><p>所有事务按顺序执行，同一时间只有一个事务能操作目标表&#x2F;行；如果检测到事务间的冲突（如事务 A 持有 S 锁，事务 B 申请 X 锁），则后申请的事务会等待，直到前一个事务结束；若等待超时，会触发死锁或超时错误。</p><h3 id="锁的退化与扩展"><a href="#锁的退化与扩展" class="headerlink" title="锁的退化与扩展"></a>锁的退化与扩展</h3><p>假设表结构：<code>id</code> (主键), <code>age</code> (普通索引)。</p><p>现有 <code>id</code> 数据：10, 20, 30。</p><h4 id="场景-1：唯一索引等值查询"><a href="#场景-1：唯一索引等值查询" class="headerlink" title="场景 1：唯一索引等值查询"></a>场景 1：唯一索引等值查询</h4><p>规则：<strong>Next-Key Lock 退化为 Record Lock</strong></p><ul><li><strong>SQL</strong>：<code>SELECT * FROM t WHERE id = 20 FOR UPDATE;</code></li><li><strong>加锁行为</strong>：仅锁定 <code>id = 20</code> 这一行记录。</li><li><strong>优化原理</strong>：由于主键&#x2F;唯一索引具有 <strong>唯一性约束</strong>，数据库可以确定该位置不会被插入重复数据（即不会发生幻读）。因此，锁机制自动优化，取消间隙锁，仅保留行锁。</li></ul><h4 id="场景-2：非唯一索引等值查询"><a href="#场景-2：非唯一索引等值查询" class="headerlink" title="场景 2：非唯一索引等值查询"></a>场景 2：非唯一索引等值查询</h4><p>规则：<strong>Next-Key Lock 扩展（向右遍历加 Gap Lock）</strong></p><ul><li><strong>SQL</strong>：<code>SELECT * FROM t WHERE age = 20 FOR UPDATE;</code></li><li><strong>加锁行为</strong>：<ol><li><strong>命中记录</strong>：对 <code>age = 20</code> 加 Next-Key Lock，锁定区间 <code>(10, 20]</code>。</li><li><strong>向右扩展</strong>：由于索引不唯一，InnoDB 需要防止在 <code>20</code> 后面插入新的 <code>20</code>。因此继续向右遍历，直到找到第一个 <strong>不等于 20</strong> 的索引值（即 <code>30</code>）。</li><li><strong>封锁间隙</strong>：对 <code>(20, 30)</code> 加 Gap Lock（注意是开区间，不锁 30）。</li></ol></li><li><strong>最终锁范围</strong>：索引区间 <code>(10, 30)</code> + 记录 <code>age = 20</code>。</li><li><strong>结论</strong>：普通索引等值查询时，锁范围 &#x3D; <strong>前间隙 + 记录本身 + 后间隙</strong>。</li></ul><h4 id="场景-3：唯一索引范围查询"><a href="#场景-3：唯一索引范围查询" class="headerlink" title="场景 3：唯一索引范围查询"></a>场景 3：唯一索引范围查询</h4><p>规则：<strong>不退化，严格遵循 Next-Key Lock</strong></p><ul><li><strong>SQL</strong>：<code>SELECT * FROM t WHERE id &gt; 20 FOR UPDATE;</code></li><li><strong>加锁行为</strong>：<ol><li>锁定区间 <code>(20, 30]</code>。</li><li>锁定区间 <code>(30, +∞)</code>。</li></ol></li><li><strong>原理</strong>：范围查询需要保证在该范围内不出现新的“幻影”记录，因此即使是唯一索引，也必须加间隙锁，严格遵循 Next-Key Lock 规则。</li></ul><h4 id="场景-4：查询不存在的记录"><a href="#场景-4：查询不存在的记录" class="headerlink" title="场景 4：查询不存在的记录"></a>场景 4：查询不存在的记录</h4><p>规则：<strong>Next-Key Lock 退化为 Gap Lock</strong></p><ul><li><strong>SQL</strong>：<code>SELECT * FROM t WHERE id = 15 FOR UPDATE;</code></li><li><strong>加锁行为</strong>：<ol><li><strong>定位</strong>：系统查找 <code>id = 15</code> 失败，定位到其右侧第一个存在的记录 <code>id = 20</code>。</li><li><strong>初始锁</strong>：<strong>理论上</strong> 应在 <code>(10, 20]</code> 加 Next-Key Lock。（这里的“理论上”指的是在 RR 级别下默认按照 Next-Key Lock 方式进行加锁）</li><li><strong>退化</strong>：因为查询值 <code>15</code> 不存在（不等于 <code>20</code>），Next-Key Lock 退化为 Gap Lock。</li></ol></li><li><strong>最终锁范围</strong>：锁定区间 <code>(10, 20)</code>。</li><li><strong>效果</strong>：锁住了 <code>10</code> 到 <code>20</code> 之间的缝隙，防止其他事务插入 <code>15</code>，但不影响 <code>id = 20</code> 这行记录本身的读写。</li></ul><h3 id="死锁问题"><a href="#死锁问题" class="headerlink" title="死锁问题"></a>死锁问题</h3><p>Gap Lock 到底挡住了什么？它挡住的是 <strong>插入意向锁</strong>。插入意向锁是一种特殊的 <strong>间隙锁</strong>，由 <code>INSERT</code> 操作在真正插入行之前设置。它表示“我想在这个间隙插入数据”。如果不存其他事务的 Gap Lock，插入意向锁直接成功；如果存在 Gap Lock，插入意向锁被阻塞，进入等待状态。</p><p><strong>Gap Lock 之间是不冲突的</strong>！。</p><p>事务 A 执行 <code>SELECT ... WHERE id = 15 FOR UPDATE</code>（不存在的行）。A 获得 <code>(10, 20)</code> 的 Gap Lock。</p><p>事务 B 执行 <code>SELECT ... WHERE id = 16 FOR UPDATE</code>（不存在的行）。B 也获得 <code>(10, 20)</code> 的 Gap Lock。<strong>注意：这里不会阻塞！因为 Gap Lock 只防插入，不防其他 Gap Lock。</strong></p><p>事务 A 尝试执行 <code>INSERT INTO t VALUES(15)</code>。A 试图加“插入意向锁”，被 B 的 Gap Lock 阻塞。</p><p>事务 B 尝试执行 <code>INSERT INTO t VALUES(16)</code>。B 试图加“插入意向锁”，被 A 的 Gap Lock 阻塞。</p><p><strong>结果</strong>：死锁。</p><p><strong>Server 层的元数据锁（MDL）</strong>。是导致生产环境“全站卡死”的头号元凶。</p><p><strong>机制</strong>：</p><ul><li>执行 DML（增删改查）时，自动申请 MDL 读锁。</li><li>执行 DDL（<code>ALTER TABLE</code> 等）时，自动申请 MDL 写锁。</li><li><strong>MDL 读写互斥</strong>。</li></ul><p><strong>灾难级阻塞链</strong>：</p><ol><li><strong>Session A</strong>：开启事务，执行 <code>SELECT</code>（长查询）。<strong>持有 MDL 读锁</strong>。</li><li><strong>Session B</strong>：执行 <code>ALTER TABLE</code> 加字段。需要 <strong>MDL 写锁</strong>。<ul><li><em>状态</em>：被 A 阻塞，进入 <code>Waiting for table metadata lock</code> 队列。</li><li><em>关键点</em>：写锁的优先级极高，它会阻塞后续所有申请读锁的请求。</li></ul></li><li><strong>Session C, D, E…</strong>：执行简单的 <code>SELECT</code>。<ul><li><em>状态</em>：<strong>全部被 B 阻塞</strong>。</li></ul></li></ol><p><strong>后果</strong>：Session A 的一个慢查询，诱发了 Session B 的 DDL 阻塞，进而导致 Session C&#x2F;D&#x2F;E（原本毫秒级的查询）全部堆积。连接池（Connection Pool）在几秒内被打爆，业务全线崩溃。</p><p><strong>MySQL 自身无法从逻辑上“根除”这种死锁</strong>，因为这是为了保证数据一致性（防止幻读）所必须付出的代价。</p><p>当死锁发生时，MySQL 采取的是 <strong>“事后补救”</strong>（检测并回滚）的策略；而作为开发者，我们需要做的是 <strong>“事前预防”</strong>（通过最佳实践规避）。</p><p>MySQL InnoDB 引擎并不能预测你会写出导致死锁的代码，它只能在死锁发生后进行干预。</p><p>InnoDB 默认开启 <strong>死锁检测</strong>（参数 <code>innodb_deadlock_detect = on</code>）。</p><ul><li><strong>机制</strong>：InnoDB 会在内存中维护一张“锁等待图”（Wait-for Graph）。这是一个有向图，节点是事务，边表示等待关系。</li><li><strong>检测</strong>：每当一个事务请求锁被阻塞时，系统会快速检查这张图是否存在 <strong>环路</strong>（Cycle）。如果存在环（A 等 B，B 等 A），判定为死锁。</li><li><strong>处理</strong>：一旦检测到死锁，InnoDB 会选择一个 <strong>“牺牲者” (Victim)</strong>。</li><li><strong>选择标准</strong>：通常选择持有锁最少、修改行数最少、回滚成本最低的那个事务。</li><li><strong>动作</strong>：强制回滚该事务，释放它持有的所有锁。</li><li><strong>结果</strong>：另一个事务获得锁，继续执行。</li></ul><p><strong>如果关闭了死锁检测</strong>（极高并发下为了减少 CPU 检测开销），MySQL <strong>依靠超时来解决</strong>。参数是 <code>innodb_lock_wait_timeout</code>（默认 50 秒）。如果一个事务等待锁超过 50 秒，直接报错退出。这是一种保底手段。</p><p>互联网大厂最常用的方案是将隔离级别降级为 RC。</p><p>Gap Lock 和 Next-Key Lock 主要是为了解决 RR 级别下的幻读问题。RC 级别下，InnoDB 只剩行锁，不再加间隙锁。上述的 Gap Lock 死锁场景直接消失。</p><p>但代价是 <strong>幻读可能发生（但在绝大多数互联网业务中是可接受的）</strong>。而且 <strong>必须配合 Binlog 格式</strong>——也就是必须将 <code>binlog_format</code> 设置为 <code>ROW</code> 模式。因为在 RC 级别下，Statement 格式的 Binlog 会导致主从数据不一致。</p><p>其实 <strong>互联网公司选择使用 RC 的主要目的是提升并发</strong>。如上所说，<strong>RC 在加锁的过程中，是不需要添加 Gap Lock 和 Next-Key Lock 的，只对要修改的记录添加行级锁就行了。</strong> 这就使得并发度要比 RR 高很多。另外，因为 <strong>RC 还支持 “半一致读”，可以大大的减少了更新语句时行锁的冲突；对于不满足更新条件的记录，可以提前释放锁，提升并发度</strong>。</p><p>如果必须使用 RR 隔离级别，请确保 <code>FOR UPDATE</code> 操作都能命中 <strong>唯一索引</strong> 且是 <strong>等值查询</strong>。</p><p>不要使用“先检查是否存在，不存在则插入”的逻辑（即 Check-Then-Act）。</p><p>错误的写法（易死锁）：</p><figure class="highlight sql"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br></pre></td><td class="code"><pre><code class="hljs sql"><span class="hljs-comment">-- 事务 A 和 B 都跑这段代码</span><br><span class="hljs-keyword">SELECT</span> <span class="hljs-operator">*</span> <span class="hljs-keyword">FROM</span> t <span class="hljs-keyword">WHERE</span> id <span class="hljs-operator">=</span> <span class="hljs-number">10</span> <span class="hljs-keyword">FOR</span> <span class="hljs-keyword">UPDATE</span>;<br><span class="hljs-comment">-- 判断结果为空</span><br><span class="hljs-keyword">INSERT INTO</span> t <span class="hljs-keyword">VALUES</span> (<span class="hljs-number">10</span>); <span class="hljs-comment">-- 死锁爆发点</span><br></code></pre></td></tr></table></figure><p>推荐的写法（利用唯一约束）：直接利用数据库的唯一索引约束来保证原子性。</p><figure class="highlight sql"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br></pre></td><td class="code"><pre><code class="hljs sql"><span class="hljs-comment">-- 直接尝试插入</span><br><span class="hljs-keyword">INSERT INTO</span> t (id, name) <span class="hljs-keyword">VALUES</span> (<span class="hljs-number">10</span>, <span class="hljs-string">&#x27;abc&#x27;</span>);<br><span class="hljs-comment">-- 捕获异常</span><br><span class="hljs-comment">-- 如果报错 Duplicate Key，说明已存在，则改为 UPDATE 或忽略</span><br></code></pre></td></tr></table></figure><p>或者使用 <code>INSERT ... ON DUPLICATE KEY UPDATE</code>：</p><figure class="highlight sql"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br></pre></td><td class="code"><pre><code class="hljs sql"><span class="hljs-keyword">INSERT INTO</span> t (id, name) <span class="hljs-keyword">VALUES</span> (<span class="hljs-number">10</span>, <span class="hljs-string">&#x27;abc&#x27;</span>) <br><span class="hljs-keyword">ON</span> DUPLICATE KEY <span class="hljs-keyword">UPDATE</span> name <span class="hljs-operator">=</span> <span class="hljs-string">&#x27;abc&#x27;</span>;<br></code></pre></td></tr></table></figure><p><strong>注意</strong>：<code>ON DUPLICATE KEY UPDATE</code> 在高并发下依然可能产生死锁（虽然概率低很多），因为它在检测到冲突时也会加锁，但比显式的 <code>FOR UPDATE</code> 更安全。</p><p>应当尽量避免范围更新，减少 <code>UPDATE ... WHERE id &gt; 10</code> 这种范围操作。范围更新会锁定大量间隙，极易误伤其他正常的插入操作。</p><p>既然 RC 这么好，为什么 MySQL 官方几十年不改默认值？主要是因为 <strong>Replication（主从复制）的历史遗留问题</strong>。</p><p>在 MySQL 5.0 以前，Binlog 只有一种格式：<strong>STATEMENT</strong>。<strong>ROW 格式</strong> 在 MySQL 5.1 才引入。</p><p>而 <strong>RC + Statement 格式 &#x3D; 主从数据不一致</strong>。</p><p>假设有表 <code>t</code> (id, age)，数据为 (10, 10), (20, 20)。</p><p><strong>场景</strong>：</p><ul><li><strong>Session A (RC)</strong>: <code>DELETE FROM t WHERE age &gt; 10;</code> (执行中，未提交)。在 RC 下，没有 Gap Lock，它只锁住了 id &#x3D; 20 这一行，<strong>没有锁住 (10, 20) 之间的间隙</strong>。</li><li><strong>Session B (RC)</strong>: <code>INSERT INTO t VALUES (15, 15);</code> (立即执行成功，因为没有 Gap Lock 挡路)</li><li><strong>Session B</strong>: <code>COMMIT;</code></li><li><strong>Session A</strong>: <code>COMMIT;</code></li></ul><p><strong>结果分析</strong>：</p><ul><li><strong>主库（Master）</strong>：<ol><li>A 先删除了 id &#x3D; 20。</li><li>B 插入了 id &#x3D; 15。</li><li>结果：主库里有 id &#x3D; 10, id &#x3D; 15。</li></ol></li><li><strong>Binlog 记录（Statement 格式）</strong>：Binlog 是按事务 <strong>提交顺序</strong> 记录的。B 先提交，A 后提交。</li><li><strong>从库（Slave）回放</strong>：<ul><li>先插入 id &#x3D; 15。</li><li>执行删除 <code>age &gt; 10</code>。因为 id &#x3D; 15 满足条件，所以 id &#x3D; 15 也被删了！</li><li><strong>结果</strong>：从库里只有 id &#x3D; 10。</li></ul></li></ul><p><strong>主从数据不一致了</strong>！</p><p>虽然现在大家普遍使用 <code>binlog_format = ROW</code>（这解决了 RC 下的不一致问题），但 MySQL 为了保证对旧版本、旧配置（默认 Statement）的 <strong>最大兼容性</strong>，不敢贸然修改默认隔离级别。</p>]]></content>
    
    
    <categories>
      
      <category>数据库技术</category>
      
    </categories>
    
    
    <tags>
      
      <tag>并发控制</tag>
      
      <tag>锁机制</tag>
      
      <tag>MVCC</tag>
      
    </tags>
    
  </entry>
  
  
  
  <entry>
    <title>MySQL逻辑架构与存储</title>
    <link href="/2026/01/21/MySQL%E9%80%BB%E8%BE%91%E6%9E%B6%E6%9E%84%E4%B8%8E%E5%AD%98%E5%82%A8/"/>
    <url>/2026/01/21/MySQL%E9%80%BB%E8%BE%91%E6%9E%B6%E6%9E%84%E4%B8%8E%E5%AD%98%E5%82%A8/</url>
    
    <content type="html"><![CDATA[<p>MySQL 最本质的特征在于其 <strong>逻辑架构与存储实现的完全解耦</strong>。这种分层架构决定了 MySQL 的生态灵活性，但也引出了独有的性能挑战与架构权衡。</p><h2 id="分层"><a href="#分层" class="headerlink" title="分层"></a>分层</h2><p>MySQL 的 <strong>宏观架构自上而下严格划分为四层</strong>，<strong>层级间通过标准的 API 接口进行交互</strong>。</p><p>整体执行流程：<strong>客户端请求 → 网络连接层处理 → Server 层解析&#x2F;优化&#x2F;执行 → 存储引擎层做数据读写 → 文件系统层落地磁盘</strong>。</p><p>各层职责严格解耦，Server 层不关心数据如何存储，仅通过统一 API 向存储引擎发指令；存储引擎不关心 SQL 如何解析，仅负责数据的物理读写和专属特性（事务、锁、索引等）实现。</p><h3 id="连接层"><a href="#连接层" class="headerlink" title="连接层"></a>连接层</h3><p>是 MySQL 与客户端的交互入口，<strong>所有存储引擎共享</strong>，核心负责 <strong>网络通信、连接管理、安全验证</strong>，屏蔽底层网络细节，让 Server 层专注处理 SQL 逻辑。</p><p>客户端（如应用程序）通过特定的数据库驱动（如 JDBC, Python mysql-connector, Go-SQL-driver）发起请求。驱动层负责将应用层的逻辑请求封装为符合 <strong>MySQL Client&#x2F;Server Protocol</strong> 的数据包。</p><ul><li><strong>TCP&#x2F;IP 协议</strong>：最通用的通信方式，支持跨物理机连接。</li><li><strong>Unix Domain Socket</strong>：仅限于 Linux&#x2F;Unix 环境下的 <strong>本机连接</strong>。它绕过了网络协议栈（无需经过 TCP 握手和校验），通过文件系统直接通信，性能优于 TCP&#x2F;IP，能显着减少 CPU 开销。</li><li><strong>Named Pipes</strong>：Windows 环境下的本机通信机制，但在生产环境中较少使用。</li></ul><p>当一个请求到达监听端口（默认 3306）时，连接层会按以下流程处理：</p><ul><li>TCP 握手与认证：连接器首先处理 TCP 三次握手，随后执行基于 <strong>挑战-响应机制</strong> 的密码校验。</li><li>权限快照：一旦连接建立并认证成功，连接器会读取当前用户的所有权限配置，并将其暂存在该会话的内存中。<strong>后续该连接内的所有操作权限校验，均以连接建立时刻 的快照为准</strong>。如果在管理员端修改了某个用户的权限，<strong>已经存在的连接不会感知到这一变化</strong>。只有当客户端断开重连后，新权限才会生效。</li><li>连接维持与超时：<ul><li>连接器负责维持连接状态。若客户端在 <code>wait_timeout</code> 指定的时间内（默认 8 小时）无交互，服务端将自动断开连接并回收资源。</li><li>由于建立连接（TCP 握手、权限验证）成本较高，工程上普遍推荐使用长连接（或连接池）来复用资源。然而，MySQL 在执行查询过程中申请的临时内存主要绑定在连接线程上，<strong>只有在连接断开时才会释放</strong>。这意味着长连接积累久了，极易导致服务器内存占用（OOM）飙升。仅依赖默认的超时机制往往不够。现代最佳实践建议采用以下两种方式之一优化内存：<ul><li><strong>定期轮转</strong>：在应用层控制长连接的使用次数（如执行 1000 次查询后主动断开重连）；</li><li><strong>轻量级重置</strong>：使用 <code>mysql_reset_connection</code> 接口。它能在 <strong>不重新进行 TCP 握手和认证</strong> 的前提下，将连接恢复到初始状态并释放堆积的内存，实现了性能与资源的平衡。</li></ul></li></ul></li></ul><blockquote><p>挑战-响应机制是一种 <strong>身份认证协议</strong>，简单来说：<strong>不直接问你“密码是多少”，而是给你一道“数学题（挑战）”，只有知道正确密码的人，才能算出正确答案（响应），验证方通过核对答案确认身份</strong>。</p><p>目的：解决 <strong>明文密码传输的泄露风险</strong>——如果直接把密码从客户端发往服务端，网络中任何能抓包的节点都能直接获取密码；而挑战-响应机制中，网络上只传输“挑战内容”和“加密后的响应结果”，即使被抓包，攻击者也无法反推出原始密码。</p></blockquote><p>MySQL 如何处理并发连接，直接决定了其在高并发下的性能表现。</p><ul><li>原生模型：单连接单线程：MySQL 的传统模型。每当有一个新的客户端连接进来，Server 就会分配一个独立的线程专门服务该连接。<ul><li><strong>优势</strong>：实现简单，上下文隔离性好。</li><li><strong>劣势</strong>：随着连接数暴涨（例如几千个连接），线程数同步暴涨。操作系统的 <strong>上下文切换</strong> 开销会耗尽 CPU 资源，导致系统吞吐量断崖式下跌。</li></ul></li><li>优化一：线程缓存：这是 MySQL 原生的基础优化。当客户端断开连接时，线程不会立即销毁，而是放回缓存区。当新连接到来时，优先从缓存中复用线程。<ul><li><strong>注意</strong>：这只是减少了线程 <strong>创建&#x2F;销毁</strong> 的开销，并没有解决高并发下线程 <strong>运行</strong> 时的上下文切换问题。</li></ul></li><li>优化二：线程池：这是解决“连接数暴涨”的终极方案。<ul><li><strong>原理</strong>：采用 N: M 模型。无论前端有多少个连接，后端只维持少量的活跃工作线程。空闲的连接请求会在队列中排队，等待工作线程处理。</li><li><strong>价值</strong>：将 <strong>连接管理</strong> 与 <strong>执行线程</strong> 解耦，确保 CPU 始终在全速处理 SQL 逻辑，而不是忙于调度线程。</li></ul></li></ul><p>在未使用线程池功能的标准社区版 MySQL 环境中，生产架构通常会在应用层与数据库之间引入 Proxy（如 ProxySQL, MyCat）。这些中间件充当了外部连接池的角色，通过 <strong>连接复用</strong> 技术，确保传输到后端 MySQL 的并发连接数始终控制在安全范围内。</p><h3 id="Server-层"><a href="#Server-层" class="headerlink" title="Server 层"></a>Server 层</h3><p>是 MySQL 的 <strong>大脑</strong>，采用 <strong>无状态</strong> 设计。它 <strong>不涉及数据的物理存储细节</strong>（那是存储引擎的事），所有存储引擎都依赖 Server 层的公共能力，核心负责 <strong>SQL 的解析、优化、执行</strong>，以及一些公共功能。</p><p>这一层是 MySQL 原生实现的，无法替换，Server 层定义了一套通用的 <strong>Handler API</strong>，任何存储引擎只要适配了这套接口，就能无缝接入。对 Server 层而言，它不需要知道底层是 B+ 树还是 LSM 树，只管调用 <code>handler::open_table</code> 或 <code>handler::rnd_next</code>。</p><p>连接建立后，SQL 语句会进入 Server 层的处理流程。</p><p>当 <strong>查询缓存</strong> 未命中时，SQL 语句首先进入编译阶段。<strong>解析器（Parser）</strong> 会对其进行 <strong>词法和语法分析</strong>，将 SQL 文本流转换为 <strong>抽象语法树（AST）</strong></p><ul><li><strong>词法分析 (Lexical Analysis)</strong>：将连续的 SQL 字符流拆解为具有独立意义的最小单元 —— <strong>词元 (Token)</strong>。例如，<code>SELECT * FROM user</code> 会被识别为 <code>Token_SELECT</code>, <code>Token_ASTERISK</code>, <code>Token_FROM</code>, <code>Token_String(user)</code>。</li><li><strong>语法分析</strong>：基于 SQL 语法规则，将词元组合成一棵 AST。如果 SQL 语句存在语法错误（如关键字拼写错误、括号不匹配），解析器会在此阶段抛出 <code>ERROR 1064 (42000)</code>，流程终止。</li><li><strong>语义分析</strong>：这是 8.0 之后的强化步骤。语法正确不代表逻辑可行，解析器会进一步检查：<ul><li>表名和列名是否存在？</li><li>是否存在列名歧义？</li><li>检查用户是否拥有相应的操作权限。</li></ul></li><li>前面全部通过后，Parser 会将最终生成的 AST 传递给优化器</li></ul><blockquote><p>在 8.0 之前，在解析前会检查 <strong>查询缓存</strong>。但由于其粒度过粗，任何对表的 <code>INSERT</code>, <code>UPDATE</code>, <code>DELETE</code> 操作都会导致所有与该表相关的查询缓存 <strong>全部失效</strong>，在写操作频繁的应用中，缓存命中率极低，反而增加了维护开销和锁竞争。因此，<strong>MySQL 8.0 已彻底移除了查询缓存</strong>。</p></blockquote><p><strong>优化器 (Optimizer)</strong> 拿到 AST 后，其目标并非“最快”，而是 <strong>“成本（Cost）最低”</strong>。它会将声明式 SQL（告诉我要什么）转换为过程式执行计划（告诉我怎么做）。这是 MySQL 性能的关键，它不一定会按照 SQL 书写的顺序执行。</p><p><strong>逻辑优化</strong>：即 <strong>等价变换</strong>，在不改变查询结果的前提下，通过重写 SQL 结构来简化计算。</p><ul><li><strong>常量折叠</strong>：<code>WHERE age &gt; 10+5</code> 自动转换为 <code>WHERE age &gt; 15</code>。</li><li><strong>外连接消除</strong>：若 <code>LEFT JOIN</code> 的 WHERE 条件排除了 NULL 值，则自动转换为 <code>INNER JOIN</code> 以获取更多优化空间。</li><li><strong>谓词下推 (Predicate Pushdown)</strong>：将 WHERE 条件尽可能“推”到离数据源最近的地方。例如在 Join 前先过滤单表数据，减少参与 Join 的记录数。</li><li><strong>子查询转换</strong>：将 <code>IN</code> 子查询尝试转换为 <code>SEMI-JOIN</code> 或 <code>EXISTS</code>，避免相关子查询带来的性能灾难。</li></ul><p><strong>物理优化</strong>：即 <strong>基于成本的优化 (CBO, Cost-Based Optimization)</strong>。优化器根据统计信息（Statistics，如行数、索引基数、数据页分布）为一个查询计算不同执行路径的 Cost（Cost &#x3D; IO 成本 + CPU 成本）。</p><ul><li><p><strong>访问路径选择</strong>：</p><ul><li><strong>全表扫描</strong>：当查询条件区分度低（如 <code>gender=&#39;male&#39;</code>）或表很小时，直接扫表往往比读索引回表更在廉价。</li><li><strong>索引扫描</strong>：当查询命中高区分度索引（如 <code>id=1</code>）时，优先走 B+ 树。</li></ul></li><li><p><strong>Join 算法选择</strong>：</p><ul><li><strong>Index Nested-Loop Join (NLJ)</strong>：利用被驱动表的索引进行循环匹配。</li><li><strong>Block Nested-Loop Join (BNL)</strong>：旧版本中，若被驱动表无索引，使用内存块缓存驱动表数据进行匹配。</li><li><strong>Hash Join</strong>：<strong>MySQL 8.0.18</strong> 引入的重大特性。在处理无索引的大表 Join 时，Hash Join 比传统的 BNL 快数倍甚至数十倍。</li></ul></li><li><p><strong>Join 顺序决策</strong>：并不总是“小表驱动大表”，而是“过滤后结果集（Fan-out）最小的表”作为驱动表。</p></li></ul><p><strong>执行器 (Executor)</strong> 是执行计划的消费者。MySQL 传统的执行模型是经典的 <strong>火山模型 (Volcano Model)</strong>，也称为迭代器模型。</p><ul><li><p><strong>执行流程</strong>：执行器并不直接操作数据文件，它通过调用存储引擎的 Handler API 接口。</p><ul><li>调用 <code>handler::read_first_row</code> 获取第一行。</li><li>符合 WHERE 条件？符合则放入结果集（Net Buffer），不符合则丢弃。</li><li>调用 <code>handler::read_next_row</code> 获取下一行，循环往复，直到 <code>handler</code> 返回 <code>EOF</code>。</li></ul></li><li><p><strong>行数统计差异</strong>：</p><ul><li><code>Rows_examined</code>（扫描行数）：引擎层实际扫描并返回给 Server 层的数据量。</li><li><code>Rows_sent</code>（发送行数）：Server 层经过进一步过滤（如 <code>Using where</code>）后最终发送给客户端的数量。</li><li>两者差距越大，说明索引的过滤性越差，Server 层做了大量无用功。</li></ul></li></ul><h3 id="存储引擎层"><a href="#存储引擎层" class="headerlink" title="存储引擎层"></a>存储引擎层</h3><p>这是数据的物理落地层，<strong>负责数据的实际存储和检索</strong> 以及 <strong>引擎专属的特性实现</strong>（事务、行锁、MVCC、Redo&#x2F;Undo Log、索引结构等）。采用 <strong>可插拔架构</strong>，数据按表存储，每个表可以指定不同的存储引擎。</p><blockquote><p>本篇不涉及日志系统</p></blockquote><p><strong>主流引擎</strong>:</p><ul><li><strong>InnoDB</strong>：默认引擎。<ul><li><strong>特性</strong>：<strong>索引即数据</strong>。完整支持 <strong>ACID 事务</strong>；支持 <strong>行级锁</strong>；引入 <strong>外键约束</strong>。</li><li><strong>核心组件</strong>：<ul><li><strong>Buffer Pool</strong>：不仅缓存数据，还缓存索引、插入缓冲（Change Buffer）、自适应哈希索引（AHI）。</li><li><strong>Doublewrite Buffer</strong>：解决“页断裂”（Partial Page Write）问题，保证数据可靠性。</li></ul></li></ul></li><li><strong>MyISAM</strong>：MySQL5.5 前默认。堆表结构，索引存偏移量，数据追加写。<strong>只支持表锁</strong>，读写互斥，高并发下性能瓶颈严重；<strong>不支持事务</strong>；崩溃后恢复能力极其简陋。<strong>纯读&#x2F;读多写极少</strong> 的静态数据场景下还有用处，如历史报表、归档数据、静态页面的数据库存储。</li><li><strong>Memory</strong>：基于内存的 Hash 索引（默认）或 B-Tree 索引。数据断电丢失；表级锁。现代架构中通常被 Redis 等外部缓存或 8.0 内部的 TempTable 引擎替代。</li></ul><p>物理隔离：各引擎的数据文件、日志机制、内存结构完全独立。</p><ul><li><strong>InnoDB</strong>：数据和索引存储在 <code>.ibd</code> 文件中（独立表空间模式），或者共享表空间 <code>ibdata</code> 文件中。</li><li><strong>MyISAM</strong>：索引（<code>.MYI</code>）与数据（<code>.MYD</code>）文件物理分离。</li><li><strong>Memory</strong>：仅在磁盘保留 <code>.sdi</code>（8.0+）或 <code>.frm</code>（旧版）表结构定义，数据纯内存。</li></ul><p>动态加载：MySQL 支持在运行时通过 INSTALL PLUGIN 命令加载新的引擎，无需重启服务。</p><blockquote><p>尽管 MySQL 允许单实例内混合使用不同引擎，但在生产环境中强烈不推荐。这会导致复杂的备份策略（因为无法通过单一逻辑建立一致性快照），且无法实现跨引擎的事务一致性。</p></blockquote><p>存储引擎并非直接操作磁盘，而是通过内存缓冲区 (Buffer Pool)进行交互，这是数据库性能的生命线。</p><ul><li><p>Buffer Pool：这是 InnoDB 最大的内存区域。当执行器请求“读取某行数据”时：</p><ul><li><strong>命中 (Hit)</strong>：若数据页已在 Buffer Pool 中，直接内存读取，微秒级响应。</li><li><strong>未命中 (Miss)</strong>：触发磁盘 I&#x2F;O，将原本的 16KB 数据页加载到 Buffer Pool，并基于 <strong>LRU 算法</strong>（Least Recently Used）淘汰冷数据。</li></ul></li><li><p>预读机制 (Read-Ahead)：为了减少 I&#x2F;O 次数，InnoDB 可能会根据局部性原理，预测性地将相邻的数据页一并加载到 Buffer Pool 中。</p></li></ul><p>根据优化器选定的执行计划，引擎层提供不同的数据检索方式：</p><ul><li><strong>全表扫描</strong>：存储引擎从表的第一个数据页开始，逐页扫描，直到表的末尾，返回所有满足 <code>WHERE</code> 条件（如果下推了）的行。</li><li><strong>索引扫描</strong>：<ul><li><p>全表扫描：引擎通过链表指针从第一个数据页遍历到最后一个数据页。</p><ul><li><strong>代价</strong>：极高的 I&#x2F;O 开销，且会污染 Buffer Pool（将热数据挤出）。</li></ul></li><li><p><strong>聚簇索引扫描 (Clustered Index)</strong>：在 InnoDB 中，主键索引的叶子节点直接存储了完整的行数据。查到索引即查到数据。</p></li><li><p><strong>二级索引扫描与回表</strong>：非主键索引的叶子节点存储的是 <strong>索引列值 + 主键 ID</strong>。</p><ul><li><strong>回表 (Lookup)</strong>：当查询需要非索引列的数据时，引擎先查二级索引拿到主键 ID，再拿着 ID 去聚簇索引树中搜索完整行数据。</li></ul></li><li><p><strong>覆盖索引</strong>: 如果 SELECT 的所有列都包含在二级索引中，引擎直接在二级索引树中读取数据返回，<strong>无需回表</strong>。这是 SQL 优化的核心手段之一。</p></li></ul></li></ul><p>存储引擎并不一次性返回所有数据，而是根据执行器的 Handler 接口调用，将获取到的每一行数据返回给执行引擎（Server 层），<strong>按行</strong> 交付。执行引擎根据 <code>SELECT</code> 列表的要求，对数据进行处理（例如，<code>COUNT(*)</code> 进行计数、<code>ORDER BY</code> 进行排序、<code>GROUP BY</code> 进行聚合）。</p><p>最终结果集由 Server 层 格式化，并写入网络缓冲区（Net Buffer），服务器的工作线程将格式化后的数据，按照网络协议（一般是 TCP&#x2F;IP 协议），分批次地发送回客户端。客户端的数据库驱动程序接收数据包，解析协议，重组为应用程序可用的格式（例如 Java 中的 <code>ResultSet</code> 对象），应用程序通过 API 遍历结果集。</p><h3 id="文件系统层"><a href="#文件系统层" class="headerlink" title="文件系统层"></a>文件系统层</h3><p>是 MySQL 逻辑数据与物理硬件交互的边界，<strong>所有引擎共享</strong>。该层不属于 MySQL 自身的逻辑范畴，而是直接依赖于操作系统的文件系统（如 Linux 的 XFS, Ext4）来管理文件的读写、权限与锁定。</p><p>该层负责 <strong>将上层定义的 数据库、表、索引、日志 等逻辑概念，映射为操作系统可见的 物理文件</strong>。任何存储引擎的内存数据最终都必须通过系统调用在此层写入磁盘，以保证持久性。</p><p>MySQL 对文件系统的使用并非简单的 <code>read/write</code>，而是针对不同文件类型采用了精细化的 I&#x2F;O 策略。</p><ul><li><strong>Direct I&#x2F;O (O_DIRECT)</strong>：InnoDB 引擎主要使用 <code>O_DIRECT</code> 模式打开数据文件和日志文件。<ul><li><strong>目的</strong>：<strong>绕过操作系统的 Page Cache</strong>。</li><li><strong>原因</strong>：InnoDB 自身维护了强大的 Buffer Pool，如果再经过 OS Cache，会导致内存中存在两份相同的数据，造成内存浪费且增加 CPU 开销。同时，绕过 OS Cache 才能让数据库精确控制数据何时真正落盘（fsync），这是实现 ACID 事务持久性的前提。</li></ul></li><li><strong>Native AIO (Asynchronous I&#x2F;O)</strong>：MySQL 强依赖操作系统的原生异步 I&#x2F;O 能力（在 Linux 上即 <code>libaio</code>）。允许 InnoDB 线程在发起 I&#x2F;O 请求后不被阻塞，继续处理其他任务，待磁盘完成操作后通过回调通知。这对提升高并发下的写入吞吐量至关重要。</li><li><strong>标准缓冲 I&#x2F;O (Buffered I&#x2F;O)</strong>：主要用于 Server 层的部分日志（如错误日志、慢查询日志）或某些工具操作。这些操作依赖 OS 的 Page Cache 进行合并写入，以减少磁盘寻址。</li></ul><p>MySQL 8.0 进行了大幅度的元数据管理重构，文件结构与旧版本有显着差异。</p><p>如果让开发者在建表时直接指定数据存在哪块磁盘的哪个文件里，不仅极大增加了开发心智负担，也让运维在动态扩容时如履薄冰。</p><p>为了解决这个耦合问题，InnoDB 引入了 <strong>表空间</strong>。它就像是一位尽职的档案秘书：向上对开发者暴露逻辑上的“表”，向下对操作系统管理复杂的物理“文件”。</p><blockquote><p>表空间本质上是一种存储抽象，它屏蔽了底层文件系统的碎片化和复杂性，使得数据库引擎可以在一个统一的逻辑连续地址空间中进行数据调度。</p></blockquote><p>在早期，InnoDB 默认将所有表的数据、索引和元数据塞进一个共享的系统表空间（通常是 <code>ibdata1</code> 文件）。</p><p>共享表空间虽然集中，但带来了灾难性的空间回收问题——当你删除一张拥有千万级数据的表时，<code>ibdata1</code> 文件的体积并不会缩小，操作系统无法回收这些释放的磁盘空间。为了解决这个问题，InnoDB 引入了 <strong>独立表空间</strong>，让每张表拥有自己独立的 <code>.ibd</code> 文件。这样一来，执行 <code>DROP TABLE</code> 时，对应的物理文件被直接删除，磁盘空间瞬间释放。</p><p>既然有了独立的文件，我们能否直接把数据一条条往文件里塞？</p><p>这里遇到了一个经典的计算机硬件痛点：磁盘的随机 I&#x2F;O 极度缓慢。根据局部性原理，如果我们在物理上将相关联的数据连续存放，就能大幅提升预读和顺序 I&#x2F;O 的效率。</p><p>为了实现连续性，InnoDB 对物理文件进行了宏观切分，引入了 <strong>区（Extent）</strong> 的概念。</p><p>一个区是一段物理上连续分配的空间，默认大小为 1MB。如果把表空间看作是一个巨大的图书馆，区就是其中一排排标准尺寸的书架。</p><p>然而，表中的数据分为两类：构成 B+ 树叶子节点的 <strong>业务数据</strong>，和构成非叶子节点的 <strong>索引数据</strong>。如果把它们混在一个区里，扫描数据时就会产生大量不必要的跳跃。</p><p>这就引出了逻辑分组的概念——<strong>段（Segment）</strong>。</p><p>段并不直接对应某一块连续的物理磁盘，它是一个逻辑概念，用于管理状态相似的区。每创建一个索引，InnoDB 会自动生成两个段：</p><ul><li><strong>数据段（Data Segment）</strong>：管理所有的叶子节点区。</li><li><strong>索引段（Index Segment）</strong>：管理所有的非叶子节点区。</li></ul><blockquote><p>如果每创建一个段就直接分配一个 1MB 的区，那么对于只有几条配置信息的小表来说，空间浪费是极其严重的。</p><p>因此，InnoDB 在分配策略上做了一个精妙的妥协：在段刚建立时，先从表空间共用的“碎片区（Fragment Extent）”中按单页（16KB）分配，直到用满 32 页后，才会判定这是一个“大段”，从此开始按完整的区（1MB）进行分配。</p></blockquote><p>区虽然保证了宏观的连续性，但 1MB 的粒度对于内存与磁盘之间的数据交换来说太大了。因此，区被进一步等分为 64 个连续的 <strong>页（Page）</strong>，默认大小为 16KB。</p><p>页不仅是物理空间，更是内存与磁盘进行 I&#x2F;O 操作的基本单位。</p><p>在业务数据所在的 Index 页中，行记录是紧密堆叠的。当我们需要在这一页中查找一条特定的数据时，难道要从头到尾遍历吗？</p><p>为了在 16KB 的页内实现极速查找，InnoDB 抽取了数据行的主键，在页的尾部构建了一个称为“槽（Slot）”的稀疏索引数组。每 4 到 8 行数据对应一个槽。</p><p>当查询落在某页时，InnoDB 直接对这个连续的槽数组进行 <strong>二分查找</strong>，瞬间将搜索范围缩小到几行记录，最后再进行极其短暂的线性遍历。</p><p>经过层层剖析，我们终于来到了真正存储数据的行（Row）。</p><p>对于开发者而言，一行数据就是几个业务字段。但在 InnoDB 眼里，要维持事务的隔离级别（MVCC）和数据的组织，就必须在每一行强行植入隐藏的元数据：</p><ul><li><strong>DB_TRX_ID</strong>：最后一次修改该行的事务 ID。</li><li><strong>DB_ROLL_PTR</strong>：回滚指针，指向 Undo Log 中该行的历史版本。</li><li><strong>DB_ROW_ID</strong>：隐藏的自增主键（只有当表既没有主键也没有非空唯一索引时才生成）。</li></ul><p>既然系统会自动生成 DB_ROW_ID，为什么还要强制指定主键？</p><p>如果不指定主键，所有没有主键的表都会 <strong>共享同一个全局的 dict_sys.row_id 序列</strong>。在极高并发的插入场景下，这个全局共享变量会受到操作系统互斥锁（Mutex）的保护。意味着你整个数据库里所有没有主键的表，在并发写入时会互相阻塞，成为性能的绝对瓶颈。</p><p>底层环境的选择直接决定了 MySQL 的 I&#x2F;O 上限。</p><ul><li><strong>文件系统选择：XFS vs Ext4</strong>：<strong>推荐 XFS</strong>。相比 Ext4，XFS 在处理大文件、高并发 I&#x2F;O 以及 <strong>稀疏文件</strong> 方面表现更优，且删除大文件时瞬间完成，不会像 Ext4 那样引起 I&#x2F;O 抖动。</li><li><strong>磁盘调度算法</strong>：<strong>推荐 noop &#x2F; deadline &#x2F; none</strong>。在 SSD&#x2F;NVMe 时代，物理设备已具备极高的并发处理能力，应将调度策略设置为 <code>none</code>（NVMe）或 <code>noop</code>，不再需要操作系统层面的电梯算法（CFQ）进行复杂的排序合并，避免无谓的 CPU 开销。</li></ul><h2 id="架构缺陷——索引下推-ICP"><a href="#架构缺陷——索引下推-ICP" class="headerlink" title="架构缺陷——索引下推 (ICP)"></a>架构缺陷——索引下推 (ICP)</h2><h3 id="职责的过度割裂"><a href="#职责的过度割裂" class="headerlink" title="职责的过度割裂"></a>职责的过度割裂</h3><p>MySQL 的分层架构存在一个看起来有点“愚蠢”的问题，而这个问题导致了 MySQL 严重的性能损耗。</p><p>如上文所说，Server 层就像是一个“大脑”，它负责 SQL 解析、优化器生成执行计划、<strong>以及最终的 WHERE 条件过滤</strong>。引擎层就是“手脚”，它通过实现 <code>handler</code> 接口提供基础的数据存取服务。</p><p>早期接口非常原始，类似于“给我下一行数据”或“给我索引范围内的数据”。</p><p>以查询 <code>SELECT * FROM user WHERE name LIKE &#39;Chen%&#39; AND age = 20</code> 为例（联合索引为 <code>idx_name_age</code>）</p><ul><li><strong>Server 层</strong> 分析发现可以使用索引 <code>name</code> 进行范围查询。</li><li><strong>Server 层</strong> 向引擎层发送指令：扫描所有 <code>name</code> 以 ‘Chen’ 开头的索引记录。此时，<code>age=20</code> 这个条件被保留在 Server 层，未传递给引擎。</li><li><strong>引擎层</strong> 通过 B+ 树定位到 <code>name=&#39;Chen&#39;</code> 的记录。虽然引擎在读取索引叶子节点时明明可以看到 <code>age</code> 的值（例如 <code>age=30</code>），但由于接口协议未包含该条件，引擎 <strong>不会过滤</strong>。</li><li><strong>引擎层</strong> 被迫执行 <strong>回表</strong> 操作，根据主键去聚簇索引读取完整的行数据，返回给 Server 层。</li><li><strong>Server 层</strong> 接收完整行数据后，再进行 <code>IF age == 20</code> 的判断。若不匹配，则丢弃。</li></ul><p>这种设计导致了大量的 <strong>无效回表</strong>。引擎层明明手握索引中的 <code>age</code> 数据，却因为职责划分过细，必须先进行昂贵的随机 I&#x2F;O 读取整行数据，仅仅是为了让 Server 层做一个本可以在索引层就完成的判断。</p><h3 id="接口的进化：Handler-层的变革-ICP"><a href="#接口的进化：Handler-层的变革-ICP" class="headerlink" title="接口的进化：Handler 层的变革 (ICP)"></a>接口的进化：Handler 层的变革 (ICP)</h3><p>索引下推 <strong>ICP（Index Condition Pushdown）</strong> 是 MySQL 架构优化的一个里程碑，它打破了 Server 层与引擎层之间僵化的界限，允许引擎层利用“手头已有的索引数据”进行预先过滤，从而将大量的随机磁盘 I&#x2F;O 扼杀在萌芽状态。</p><p>MySQL 5.6 引入 ICP ，本质上是对 <strong>Handler 接口协议</strong> 进行了扩展。</p><p><strong>改动前的交互模式</strong>：<code>Handler：：read_range( start=&#39;Chen&#39;, end=&#39;Chen~&#39; )</code></p><p>语义：引擎，请取出该范围内的所有行记录。</p><p><strong>改动后的交互模式</strong>：<code>Handler：：read_range( start=&#39;Chen&#39;, end=&#39;Chen~&#39;, condition=&quot;age=20&quot; )</code></p><p>语义：引擎，在该范围内查找时，请 <strong>顺便</strong> 检查 <code>age</code> 是否等于 20。如果不匹配，直接跳过该索引节点，不需要回表。</p><p><strong>底层实现细节</strong>：在代码层面，MySQL 在 <code>handler</code> 类中增加了一个 <code>pushed_idx_cond</code> 成员变量。当开启 ICP 时，优化器会将部分 WHERE 条件通过这个变量“下推”给存储引擎。引擎在遍历 B+ 树索引记录时，会先计算 <code>pushed_idx_cond</code>，只有满足条件的记录，才会被加入到结果集或进行回表操作。</p><p>引擎层直接过滤掉不匹配的记录，不再向 Server 层传输无用数据。回表操作通常涉及离散的磁盘读取，ICP 极大地减少了这种昂贵的操作。</p><p>三种条件过滤级别：</p><ul><li><p><strong>Index Key (索引定位)</strong></p><ul><li><p><strong>示例</strong>：<code>col_A &gt; 10</code> (索引第一列)</p></li><li><p><strong>机制</strong>：用于确定 B+ 树搜索的 <strong>起止范围</strong>。</p></li><li><p><strong>执行者</strong>：引擎层。这是搜索的起点，不涉及过滤，而是涉及定位。</p></li></ul></li><li><p><strong>Index Filter (索引下推 ICP)</strong></p><ul><li><p><strong>示例</strong>：<code>col_B = 20</code> (索引第二列，但在第一列范围查询后)</p></li><li><p><strong>机制</strong>：根据“最左前缀原则”，范围查询后的列无法用于快速定位，但它们依然存在于索引树中。</p></li><li><p><strong>执行者</strong>：引擎层（开启 ICP 后）。</p></li><li><p><strong>行为</strong>：引擎在读取索引记录时直接比对。如果不匹配，直接读取下一条索引记录，<strong>不回表</strong>。</p></li><li><p><strong>标识</strong>：在 <code>EXPLAIN</code> 输出的 <code>Extra</code> 列中显示 <code>Using index condition</code>。</p></li></ul></li><li><p><strong>Table Filter (表级过滤)</strong></p><ul><li><p><strong>示例</strong>：<code>col_C = 30</code> (完全不在索引中)</p></li><li><p><strong>机制</strong>：数据不在当前的 B+ 树索引上，引擎层“看不到”该字段。</p></li><li><p><strong>执行者</strong>：Server 层。</p></li><li><p><strong>行为</strong>：引擎层必须先 <strong>回表</strong>，读取完整数据行返回给 Server 层，由 Server 层进行最终过滤。</p></li><li><p><strong>标识</strong>：在 <code>EXPLAIN</code> 输出的 <code>Extra</code> 列中显示 <code>Using where</code>（注意：<code>Using where</code> 并不一定代表回表，但在有 ICP 的对比下，它通常意味着 Server 层参与了过滤）。</p></li></ul></li></ul><p>并非所有情况都能利用 ICP：</p><ul><li><strong>仅适用于二级索引</strong>：ICP 的核心目的是减少回表。对于 InnoDB 的聚簇索引（主键索引），因为数据本身就和索引在一起，不存在“回表”概念，所以 ICP 意义不大。</li><li><strong>存储引擎限制</strong>：主要支持 InnoDB 和 MyISAM 引擎。</li><li><strong>引用限制</strong>：如果过滤条件中引用了存储函数，或者触发了某些复杂的逻辑，引擎层可能无法处理，依然需要由 Server 层处理。</li><li><strong>虚拟列</strong>：对于建立在虚拟列上的二级索引，ICP 同样有效。</li></ul><h2 id="分层的代价——双写一致性"><a href="#分层的代价——双写一致性" class="headerlink" title="分层的代价——双写一致性"></a>分层的代价——双写一致性</h2><p>分层架构除了会带来上述 的 <strong>“效率损耗”</strong> 问题，还引入了一个“<strong>分布式事务问题</strong>”，只不过它不是发生在多台服务器之间，而是发生在一个单机系统的 <strong>Server 层</strong> 和 <strong>引擎层</strong> 之间。</p><h3 id="两个“大脑”，两份“账本”"><a href="#两个“大脑”，两份“账本”" class="headerlink" title="两个“大脑”，两份“账本”"></a>两个“大脑”，两份“账本”</h3><p>在 MySQL 的分层架构中，Server 层和引擎层各自维护了一份日志：</p><ul><li><strong>Binlog（归档日志）</strong>：属于 Server 层（上层）。它是下游（从库、数据分析）的数据源头，决定了 <strong>“主从是否一致”</strong>。</li><li><strong>Redolog（重做日志）</strong>：属于 InnoDB 引擎层（底层）。它是崩溃恢复的唯一依据，决定了 <strong>“本机是否不丢数据”</strong>。</li></ul><p>由于这两份日志分别由不同的组件写入，如果不能保证它们 <strong>原子性</strong> 地写入成功，就会出现 <strong>数据一致性问题</strong>。</p><p>假设我们执行一个 <code>UPDATE</code> 操作：</p><ul><li><p><strong>场景 A（先写 Redolog，后写 Binlog）</strong>：Redolog 写完后系统宕机，Binlog 没来得及写。</p><ul><li><strong>结果</strong>：重启后，主库通过 Redolog 恢复了这行数据。但因为 Binlog 缺失，从库永远收不到这条更新。</li><li><strong>判定</strong>：<strong>主从不一致</strong>（主库多，从库少）。</li></ul></li><li><p><strong>场景 B（先写 Binlog，后写 Redolog）</strong>：Binlog 写完后系统宕机，Redolog 没来得及写。</p><ul><li><strong>结果</strong>：重启后，主库因为 Redolog 缺失，事务回滚（数据消失）。但从库收到了 Binlog，执行了更新。</li><li><strong>判定</strong>：<strong>主从不一致</strong>（主库少，从库多）。</li></ul></li></ul><h3 id="补丁：两阶段提交-2PC"><a href="#补丁：两阶段提交-2PC" class="headerlink" title="补丁：两阶段提交 (2PC)"></a>补丁：两阶段提交 (2PC)</h3><p>为了缝合 Server 层与引擎层之间的裂痕，MySQL 借鉴了分布式系统的思路，引入了 <strong>两阶段提交 (Two-Phase Commit, 2PC)</strong> 机制。</p><p>这是一种 <strong>跨层协作协议</strong>：</p><ol><li><strong>Prepare 阶段</strong>：引擎层将 Redolog 写入磁盘，并标记为“准备好”。</li><li><strong>Commit 阶段</strong>：Server 层写入 Binlog，然后通知引擎层将 Redolog 标记为“提交”。</li></ol><p>引入 2PC 后，MySQL 重启时的恢复逻辑不再只看引擎层，而是要 <strong>综合两层日志</strong> 进行判断：<strong>以 Binlog 是否完整为最高准则</strong>。</p><p>只要 Binlog 已经完整写入（哪怕 Redolog 还在 Prepare 状态），MySQL 也会强行提交该事务，从而保证主库与从库的数据最终一致。</p><blockquote><p>本篇不涉及 2PC 的具体执行流程、崩溃恢复的详细规则以及为了解决 2PC 性能问题而引入的 <strong>组提交</strong> 机制</p></blockquote><h2 id="从-SQL-到磁盘：一条数据的物理落盘之旅"><a href="#从-SQL-到磁盘：一条数据的物理落盘之旅" class="headerlink" title="从 SQL 到磁盘：一条数据的物理落盘之旅"></a>从 SQL 到磁盘：一条数据的物理落盘之旅</h2><p>理解了上述所有组件，现在我们可以将这套精密的机械运转起来，推演一条数据从插入到落盘的全过程：</p><ol><li><strong>寻址逻辑入口</strong>：引擎首先通过系统表空间找到 <code>user</code> 表的元数据，提取出聚集索引 B+ 树的 Root Page 编号。</li><li><strong>树的遍历</strong>：顺着 Root Page，根据主键（如果没有，分配全局互斥的 DB_ROW_ID），利用二分查找快速向下定位到目标叶子节点所在的物理数据页。</li><li><strong>页内调配</strong>：检查该数据页剩余空间。如果足够，更新 Page Directory，直接将行记录写入。</li><li><strong>空间伸缩</strong>：如果空间不足，触发页分裂。引擎会向该段管理的“未满区（Not Full Extent）”申请新的 16KB 页。如果区也满了，则向表空间的“空闲区（Free Extent）”申请整整 1MB 的新空间。</li><li><strong>持久化防线</strong>：所有修改先写入 Redo Log 并在内存中标记为脏页。在合适的时机，经过 Doublewrite Buffer 护航，最终安全地落入磁盘的 <code>.ibd</code> 文件中。</li></ol>]]></content>
    
    
    <categories>
      
      <category>数据库</category>
      
    </categories>
    
    
    <tags>
      
      <tag>MySQL</tag>
      
      <tag>架构设计</tag>
      
      <tag>存储引擎</tag>
      
    </tags>
    
  </entry>
  
  
  
  <entry>
    <title>MySQL日志全解</title>
    <link href="/2026/01/20/MySQL%E6%97%A5%E5%BF%97%E5%85%A8%E8%A7%A3/"/>
    <url>/2026/01/20/MySQL%E6%97%A5%E5%BF%97%E5%85%A8%E8%A7%A3/</url>
    
    <content type="html"><![CDATA[<p>我们的故事从一个简单的 <code>UPDATE</code> 事务开始。</p><p>为了在 <strong>极致的性能（内存）</strong> 和 <strong>可靠（磁盘）</strong> 之间找到完美的平衡，InnoDB 内部有着一套复杂且环环相扣的机制。</p><blockquote><p>到达引擎层之前还有很多步骤，本篇不涉及</p></blockquote><h3 id="Buffer-Pool"><a href="#Buffer-Pool" class="headerlink" title="Buffer Pool"></a><code>Buffer Pool</code></h3><p>Buffer Pool 是 InnoDB 在 <strong>内存中开辟的一块区域</strong>，用来 <strong>缓存磁盘上的数据页和索引页</strong>。它是 InnoDB 性能的基石。</p><p>为什么要有 Buffer Pool？</p><p>因为计算机世界存在着一条巨大的速度鸿沟——<strong>内存读写是纳秒级，而磁盘读写是毫秒级</strong>（慢几十万倍）</p><p>所以 InnoDB 为了性能，制定了一条规矩：“<strong>所有的操作，必须在内存中进行</strong>”</p><ul><li><strong>读数据：</strong> 必须先把磁盘页加载到 Buffer Pool，再由 CPU 读取。</li><li><strong>写数据：</strong> <strong>不直接写硬盘！</strong> 而是先修改 Buffer Pool 里的页。</li></ul><blockquote><p>这里就诞生了“脏页”这个概念</p><p>当我们执行 一条 <code>UPDATE</code> 语句后：</p><ol><li>InnoDB 只改了 Buffer Pool 里的页，还没同步到磁盘。</li><li>此时，内存里的数据和磁盘上的数据是不一致的。</li><li>这个时候的内存页，就叫做 <strong>“脏页”</strong>。</li></ol><p>刷盘(Flush)：InnoDB 有后台线程(如 MasterThread 或 PageCleanerThread)会在系统负载较低时，或者在特定时机(如 <code>Checkpoint</code>)，把这些“脏页”异步地写回磁盘文件。</p><p>这就有了一个 <strong>问题</strong>：如果中途断电了怎么办？内存是易失性的，里面的数据会瞬间消失。</p></blockquote><h3 id="一个-UPDATE-事务的执行"><a href="#一个-UPDATE-事务的执行" class="headerlink" title="一个 UPDATE 事务的执行"></a>一个 <code>UPDATE</code> 事务的执行</h3><p>我们的 <code>UPDATE</code> 事务登场了。它要在 <code>Buffer Pool</code> 中修改数据。</p><h4 id="第-0-步：加锁"><a href="#第-0-步：加锁" class="headerlink" title="第 0 步：加锁"></a>第 0 步：加锁</h4><p>一开始它就面临着一个严峻的挑战：<strong>并发冲突</strong>。如果两个事务同时试图修改 <code>id=1</code> 的数据，谁先谁后？</p><p>于是，InnoDB 祭出了 <strong>锁</strong> 机制，事务在执行 <code>UPDATE</code> 前，会先去向锁管理器申请锁。如果申请成功，才能继续后续的操作。如果失败，当前事务就会进入等待状态，直到超时或对方释放锁。</p><blockquote><p>本文不涉及关于并发安全问题以及锁机制的详细内容</p></blockquote><h4 id="第-1-步：记录“后悔药”-Undo-Log"><a href="#第-1-步：记录“后悔药”-Undo-Log" class="headerlink" title="第 1 步：记录“后悔药”(Undo Log)"></a>第 1 步：记录“后悔药”(<code>Undo Log</code>)</h4><p><strong>事务的原子性</strong> 是指一个事务中的操作，要么全部成功，要么全部失败。</p><p>所以如果事务执行一半失败了，或者用户执行了 <code>ROLLBACK</code>，事务就需要回滚。InnoDB 需要某种机制来撤销所有已做的修改。</p><p>这就有了我们的第一个日志，在修改数据之前，InnoDB 会先记录“如何撤销”这次修改。这就是 <strong><code>Undo Log</code> (撤销日志)</strong>。</p><p>它是一种 <strong>逻辑日志</strong>，记录的是“如何撤销刚才的修改”：</p><ul><li><code>INSERT</code> 会记录一条对应的 <code>DELETE</code>。</li><li><code>DELETE</code> 会记录一条对应的 <code>INSERT</code>。</li><li><code>UPDATE</code> 会记录一条反向的 <code>UPDATE</code>（旧值）。</li></ul><p>在修改数据之前，InnoDB 会先把数据的“旧值”记录到 <code>Undo Log</code> 中。</p><p>值得一提的是，<code>Undo Log</code> 本身也是数据，它存储在 Undo 页中，Undo 页也和数据页一样，会被缓存在 <code>Buffer Pool</code> 中，也会变“脏”。</p><h4 id="第-2-步：保护数据-Redo-Log"><a href="#第-2-步：保护数据-Redo-Log" class="headerlink" title="第 2 步：保护数据(Redo Log)"></a>第 2 步：保护数据(<code>Redo Log</code>)</h4><p>正如前文所说，内存具有易失性，一旦中途断电，BufferPool 中的数据会瞬间蒸发。</p><p>Undo Log 只能回滚，不能把丢失的数据找回来。更别提 <strong><code>Undo Log</code> 本身也是数据</strong>，它也面临着丢失的问题。</p><p>所以为了实现事务的 <strong>持久性</strong>，MySQL 需要保证，在告诉用户事务“提交成功”之前，必须 <strong>把这个修改后的 16KB 的数据页，完完整整地写回到磁盘的数据文件中</strong>。</p><p>这简直就是性能杀手：</p><ul><li>首先是 <strong>随机 I&#x2F;O</strong>：数据库的数据页是散落在磁盘的各个角落的。你修改 <code>id=1</code> 可能在磁盘头部，修改 <code>id=10000</code> 可能在磁盘尾部。每次提交事务都要磁头乱跳或者寻址，速度极慢。</li><li>其次就是 <strong>写放大</strong>： 你可能只改了一个 16KB 页面里的几个字节，但你必须把整个 16KB 的页重新写入磁盘。</li></ul><p>而且这没有解决“<strong>崩溃恢复</strong>”的问题，断电重启后，数据还是丢了。</p><p>所以为了解决这个问题，InnoDB 引入了 <strong><code>Redo Log</code> (重做日志)</strong>，它是一种 <strong>物理日志</strong>，它记录的是“对数据页做了什么修改”，比如：“在 Page5 的偏移量 100 处，写入了’ABC’”。</p><blockquote><p>目前为止，本文一直说的都是 <strong>InnoDB</strong> 引入，因为 Undo 和 Redo log 确实就是 InnoDB 独有的。</p><p>MySQL 采用的是 <strong>“可插拔”存储引擎架构</strong>，Server 层是它的“大脑”，负责“指挥”，它 <strong>不涉及数据的物理存储细节</strong>，核心负责 <strong>SQL 的解析、优化、执行</strong>，以及一些公共功能（比如全局日志（binlog），函数等等）。这一层是 MySQL 原生实现的，无法替换。</p><p>而存储引擎层是数据的物理落地层，<strong>负责数据的实际存储和检索</strong> 以及 <strong>引擎专属的特性实现</strong>，像 InnoDB 引擎就实现了 <strong>事务、行锁、MVCC、Redo&#x2F;Undo Log</strong>、 索引组织表等。Server 层定义了一套通用的 <strong>Handler API</strong>，任何存储引擎只要适配了这套接口，就能无缝接入。</p></blockquote><p>但只引入这么个东西，问题还是没有解决。</p><p>因此 InnoDB 还为它设计了一个特殊的工作机制——<strong>Write-Ahead Logging (WAL)（日志先行）</strong>。</p><p>核心原则就是：<strong>对数据的修改，必须先写日志，再写实际的数据文件</strong>。</p><ol><li>修改数据时先在 <code>Buffer Pool</code> 中修改，同时 <strong>把这个修改动作写入内存中的 <code>Redo Log Buffer</code></strong>。</li><li>在事务提交时，必须确保 <code>Redo Log</code> 已经从 <code>Redo Log Buffer</code> 刷入到磁盘上的 <code>Redo Log File</code>。</li><li>至于 <code>Buffer Pool</code> 中的“脏页”什么时候刷回磁盘，可以晚一点，不着急。</li></ol><p>只要日志写进去了，就认为数据安全了。哪怕内存崩了，重启后也能根据日志把数据“重做”一遍。</p><blockquote><p><code>innodb_flush_log_at_trx_commit</code> 控制事务提交时 <code>Redo Log</code> 的刷盘策略：</p><ul><li><code>1</code>：默认值，每次事务提交时，都必须将 <code>Redo Log</code> 从 buffer 同步写入到磁盘文件，才算提交成功。</li><li><code>0</code>：事务提交时，不立即将 <code>Redo Log</code> 写入磁盘，而是等待主线程每秒一次的刷盘操作。如果 MySQL 在这 1 秒内宕机，会丢失最多 1 秒的数据。</li><li><code>2</code>：事务提交时，只将 <code>Redo Log</code> 写入操作系统的页缓存，而不是直接写入磁盘文件。由操作系统决定何时将缓存刷到磁盘。如果只是 MySQL 进程宕机而操作系统未宕机，数据不会丢失；但如果服务器整机断电，则会丢失数据。</li></ul></blockquote><p>为什么能行？</p><ul><li>它将对数据文件的 <strong>随机 I&#x2F;O</strong>，转换成了对日志文件的 <strong>顺序 I&#x2F;O</strong>。脏页刷盘是随机分布在磁盘各处的(随机 I&#x2F;O，很慢)。但写 <code>Redo Log</code> 永远是追加写入(顺序 I&#x2F;O，快)。</li><li>只要 <code>Redo Log</code> 刷盘了，就算数据库崩溃，“脏页”没来得及刷盘，重启时 MySQL 也可以根据 <code>Redo Log</code> 把“脏页”上的修改“重做”一遍，恢复到崩溃前的状态。</li></ul><blockquote><p>其实在 InnoDB 的 <strong>物理存储层面</strong>，也存在原子性问题。</p><p>想象一下 <strong>B+ 树的分裂</strong>：当你插入一条数据，导致叶子节点满了，InnoDB 需要申请一个新页，把旧页的一半数据挪过去，修改父节点的指针，甚至可能引起父节点的分裂。</p><p>这是一系列复杂的物理操作。如果在这个过程中断电了：新页申请了但没挂到树上，或者父指针指错了地方，整棵 B+ 树的索引结构就 <strong>彻底损坏</strong> 了。</p><p>为了保证底层物理结构的完整性，InnoDB 引入了 <strong>Mini-Transaction (MTR)</strong>。</p><ul><li>一组对 B+ 树的物理修改（涉及多个页），被视为一个不可分割的单元。InnoDB 会在内存中将这组操作对应的 <code>Redo Log</code> 准备好。</li><li>在 MTR 结束时，这一批 <code>Redo Log</code> 会被 <strong>原子地</strong> 拷贝到全局的 <code>Redo Log Buffer</code> 中。你要么在 <code>Redo Log</code> 里看到完整的“页分裂”过程，要么完全看不到。绝不会出现“只分裂了一半”的日志。</li></ul></blockquote><p>但需要注意，这里的 <code>Redo Log</code> 日志以及它的 WAL 机制都 <strong>只是为了持久化而做的性能优化方案</strong>，在系统正常运行时，<code>Buffer Pool</code> 中的脏页刷回磁盘，完全不需要看 <code>Redo Log</code>。<strong><code>Redo Log</code> 只有在“非正常”时刻（崩溃恢复）才会被读取</strong>。</p><blockquote><p>那既然 <code>Redo Log</code> 是顺序读取，速度很快，为什么不干脆让后台线程去读 <code>Redo Log</code>，然后根据日志内容更新磁盘上的数据文件呢？这样岂不是连“脏页”都不用管了。</p><p>在 <strong>“正常运行”</strong> 场景下，它有三个巨大的问题：</p><p>第一个是 <strong>合并同类项效应</strong>：</p><ul><li><strong><code>Redo Log</code> 记录的是“动作”</strong>： 如果你对同一个数据页修改了 100 次，<code>Redo Log</code> 会记录 100 条日志，如果要根据 <code>Redo Log</code> 刷盘，你需要读取这 100 条日志，然后对磁盘文件执行 100 次修改操作。</li><li><strong><code>Buffer Pool</code> 里的脏页记录的是“结果”</strong>：无论你修改了 Page A 多少次，在内存里的这个脏页，永远只有 <strong>1 个</strong>。它是这 100 次修改后的最终状态。等到要刷盘的时候，只需要把这个页写 <strong>1 次</strong> 磁盘。</li></ul><p>第二点，<code>Redo Log</code> 只是把 <strong>写日志</strong> 变成了顺序 I&#x2F;O，但它并没有把 <strong>应用到数据文件</strong> 这个动作变成顺序 I&#x2F;O。即使你顺序读取了 <code>Redo Log</code>，知道了要改哪，但你要改的 <strong>目标文件</strong>（.ibd 数据文件）依然是散落在磁盘各处的，当你去更新数据文件时，磁头还是得到处跳。</p><p>相比之下，InnoDB 的后台刷脏页机制会做很多优化，比如它会检测哪些脏页在物理磁盘上是相邻的，尽量把它们一起刷，或者按照 Space ID 排序刷，这比盲目照着 <code>Redo Log</code> 刷要高效得多。</p><p>第三点，也是这个方案最不合理的一点：</p><p><code>Redo Log</code> 是 <strong>物理日志</strong>，它记录的是“在 Page X 的 Offset Y 写入了 Z”。它 <strong>不包含</strong> 整个数据页的完整内容。如果你想通过 <code>Redo Log</code> 去更新磁盘上的数据，你手里只有一条日志：“在偏移量 100 处写入 ‘A’”。你不能直接往磁盘写一个只有 ‘A’ 的文件。你必须先把 <strong>原始数据页</strong> 从磁盘读出来。把 ‘A’ 拼接到这个页里。再把这个页写回磁盘。这就尴尬了，既然这个“修改后的完整页”已经在 <code>Buffer Pool</code> 里躺着了，你直接把它写下去不就好了吗？为什么非要“读日志 -&gt; 读磁盘旧页 -&gt; 拼装 -&gt; 写盘”这么折腾一圈呢？</p></blockquote><p><code>Redo Log</code> 在磁盘上的存储形式比较特别，磁盘上的物理文件，通常命名为 <code>ib_logfile0</code>、<code>ib_logfile1</code> 等。<strong>这些文件构成一个逻辑上的环形队列</strong>，从头到尾循环写入，当写到末尾时，会回到开头覆盖旧的日志。</p><ul><li>InnoDB 使用 <code>writepos</code>(当前写入位置)和 <code>checkpoint</code>(擦除点)两个指针来管理这个环形缓冲区。</li><li><code>writepos</code> 追上 <code>checkpoint</code> 时，表示 <code>Redo Log</code> 空间耗尽。</li><li>此时 MySQL 必须 <strong>阻塞所有更新操作</strong>，强制将 <code>Buffer Pool</code> 中的脏页刷盘，以推进 <code>checkpoint</code> 释放日志空间（这被称为 “<strong>FuriosFlushing</strong>“，是性能抖动的常见原因）。</li></ul><blockquote><p>为了避免这种“性能抖动”，InnoDB 引入了 <strong>Adaptive Flushing (自适应刷盘)</strong> 算法。</p><ul><li>后台线程会持续监控两个核心指标：<strong><code>Redo Log</code> 的生成速率</strong> 和 <strong>脏页的产生速率</strong>。</li><li>InnoDB 不需要等到 <code>Redo Log</code> 快满才动手。它会根据当前的生成速率，计算出一个合适的“刷盘速度”。如果 Log 涨得快，后台线程就默默加快刷脏页的频率；如果 Log 涨得慢，就降低频率节省 I&#x2F;O。</li></ul><p>这个机制保证了数据库的 I&#x2F;O 负载是一条平滑的曲线，而不是锯齿状的波峰波谷，让业务感受不到后台的“忙碌”。</p></blockquote><h4 id="第-3-步：提交数据（Binlog-与-2PC）"><a href="#第-3-步：提交数据（Binlog-与-2PC）" class="headerlink" title="第 3 步：提交数据（Binlog 与 2PC）"></a>第 3 步：提交数据（<code>Binlog</code> 与 2PC）</h4><p>客户端执行了 <code>COMMIT</code>，提交了事务。</p><p>这里需要补充一个背景，一般来说，我们不会只有一台 MySQL，而是有多台，形成一个集群，并实现 <strong>主从复制（Replication）</strong>——就是一台 MySQL 作为主库，其它作为从库，<strong>分担读数据的压力</strong>。</p><p>MySQL 的 Server 层 <strong>通过 <code>Binlog</code> (归档&#x2F;二进制 日志) 实现主从复制</strong>。它记录了 <strong>所有对数据库造成数据更改的 DDL（Data Definition Language，数据定义语言，建表&#x2F;改结构）和 DML（Data Manipulation Language，数据操纵语言，增删改）</strong>。</p><blockquote><p>其实从这里就能看出，Binlog 必须设计在 Server 层，只有 Server 层才具备解析 SQL 的能力，它能区分什么是 DDL 什么是 DML。底层的存储引擎只负责执行具体的物理读写指令，无法感知上层的业务逻辑。</p></blockquote><p><code>Binlog</code> 是逻辑日志，而且有多种日志格式。</p><ol><li><code>STATEMENT</code> <strong>：记录原始的 SQL 语句</strong>。优点是日志文件小，<strong>缺点是在某些情况下可能导致主从数据不一致</strong>（例如，SQL 中包含 <code>UUID()</code>、<code>NOW()</code> 等不确定性函数）。</li><li><code>ROW</code> <strong>：记录每一行数据被修改的具体内容</strong>。这是目前 <strong>默认且推荐</strong> 的格式。优点是能精确地记录数据变更，保证主从一致性，但 <strong>缺点是日志文件可能会变得很大</strong>。</li><li><code>MIXED</code>：是 <code>STATEMENT</code> 和 <code>ROW</code> 的混合模式。MySQL 会根据执行的 SQL 语句，自动选择更合适的格式。一般情况下使用 <code>STATEMENT</code>，但在可能导致数据不一致时，会自动切换到 <code>ROW</code> 格式。</li></ol><p><code>Binlog</code> 的写入也是先写内存，每个线程都有一个独立的 <code>binlog cache</code>。</p><blockquote><p>如果事务过大，超过了 <code>binlog_cache_size</code> 设置的大小，MySQL 会被迫将多余的数据写入 <strong>临时文件</strong>。这会导致磁盘 I&#x2F;O 飙升。因此，对于经常有大事务（如大批量导入、大字段更新）的系统，需要适当调大此参数。</p></blockquote><p>由 <code>sync_binlog</code> 参数控制 <code>Binlog</code> 的刷盘策略：</p><ul><li><code>0</code>：<strong>默认值，但在高可用环境下不推荐</strong>。事务提交后，<code>Binlog</code> 只写入操作系统的页缓存，由操作系统决定何时刷盘。性能最好，但服务器宕机时 <code>Binlog</code> 可能会丢失。</li><li><code>1</code>：每次事务提交，都将 <code>Binlog</code> 同步写入磁盘。最安全，但对性能有一定影响。</li><li><code>N</code>（N &gt; 1）：每 N 次事务提交，才将 <code>Binlog</code> 同步写入磁盘。</li></ul><blockquote><p>所谓双 1 配置就是指的把 <code>innodb_flush_log_at_trx_commit</code> 和 <code>sync_binlog</code> 两个都配置为 1，这样每次事务提交时，都会同步把 <code>Redo Log</code> 和 <code>Binlog</code> 写入磁盘，一致性最高。</p></blockquote><p>在主从架构中，主库将其 <code>Binlog</code> 传递给从库，从库接收并重放 <code>Binlog</code> 中的事件，从而实现与主库的数据同步（主从复制）。</p><p>利用 <code>Binlog</code> 还能进行精确的 <strong>数据恢复</strong>——通过使用全量备份（如 <code>mysqldump</code>）加上 <code>Binlog</code>，可以将数据库 <strong>恢复到任意一个精确的时间点</strong>（<strong>先恢复全量备份，然后重放指定时间范围内的 <code>Binlog</code> 事件</strong>）。</p><p>以及一个让 <code>Binlog</code> 在 MySQL 里几乎无法被去掉的场景——<strong>审计（Auditing）</strong>。</p><p><strong>在现代大数据架构中，Binlog 的地位甚至已经超越了 MySQL 本身</strong>。其作用不仅是复制，更是 <strong>CDC (Change Data Capture)</strong>。</p><p>大数据（Flink&#x2F;Spark）、搜索引擎（Elasticsearch）、缓存（Redis）的数据更新，几乎全靠监听 MySQL 的 Binlog（通过 Canal 或 Debezium）。通过分析 <code>Binlog</code> 的内容，追踪数据库的所有数据变更历史，进行数据分析与问题排查。</p><blockquote><p><code>Binlog</code> 为什么不能用来做 Crash Recovery？</p><p>假设 MySQL 崩溃了。重启时，<code>Binlog</code> 里记录着：“给 ID &#x3D; 2 的 c 字段加 1”。</p><p>此时磁盘上的数据页可能有两种状态：</p><ol><li><strong>已落盘</strong>：该操作对应的脏页已经刷入磁盘。</li><li><strong>未落盘</strong>：该操作仅在内存中完成，磁盘上的数据页还是旧的。</li></ol><p>Binlog 是追加写的，它只知道操作发生了，但 <strong>不知道这个操作对应的物理数据页是否已经真正写入了磁盘</strong>。</p><p>这就是 <code>Binlog</code> 的问题所在，<strong>它没法告诉你数据页的状态</strong>。</p><p>同时因为 Binlog 是 <strong>逻辑日志</strong>。所以它 <strong>无法保证幂等性（Idempotency）</strong>。如果 MySQL 重启后强制重放 Binlog，对于“已落盘”的数据页，再次重放会导致数据被 <strong>重复修改</strong>（例如值从 2 变成 3，再变成 4），从而导致数据错乱。</p><p><code>Redo Log</code> 记录的是物理修改（其实也不是纯物理日志），所以主要还是靠数据页头部的 LSN 作为判断依据，以此来实现 Idempotency。</p></blockquote><p>既然有主从架构，那就会有主从切换。在传统的 MySQL 主从复制中，从库同步数据依赖的是“文件名 + 物理偏移量”（例如：<code>master-bin.00032</code> pos <code>45602</code>），被称为 <strong>物理坐标</strong>。这简直是运维的噩梦，因为一旦主库宕机，新主库上位，从库必须手动计算并重新指向新的物理位置。这个过程极易出错，稍有偏差就会导致数据丢失或同步中断。</p><p>现代 MySQL 引入了 <strong>GTID (全局事务标识符)</strong> 来解决这个问题——每一个事务在提交时，都会被分配一个全局唯一的 ID（<code>Server_UUID:Transaction_ID</code>），这个 ID 会随着 Binlog 传遍整个集群，且在整个生命周期内保持不变。在主从切换时，从库只需要对新主库说：“我有 GTID 1 到 100 的数据，后面的全给我。”新主库就能自动计算出缺少的数据并发送过去。GTID 屏蔽了底层的物理文件位置，让高可用架构（如 MHA、Orchestrator）的实现变得简单可靠。</p><p>在 MySQL 的分层架构中，Server 层和引擎层各自维护了一份日志：</p><ul><li><strong>Binlog（归档日志）</strong>：属于 Server 层（上层）。它是下游（从库、数据分析）的数据源头，决定了 <strong>“主从是否一致”</strong>。</li><li><strong>Redolog（重做日志）</strong>：属于 InnoDB 引擎层（底层）。它是崩溃恢复的唯一依据，决定了 <strong>“本机是否不丢数据”</strong>。</li></ul><p>由于这两份日志由不同组件维护，如果不能保证它们 <strong>原子性</strong> 地写入成功，就会出现 <strong>数据一致性问题</strong>。</p><p>这是一个发生在一个单机系统的 <strong>Server 层</strong> 和 <strong>引擎层</strong> 之间的“<strong>分布式事务问题</strong>”。</p><p>假设我们执行一个 <code>UPDATE</code> 操作：</p><ul><li><p><strong>场景 A（先写 Redolog，后写 Binlog）</strong>：Redolog 写完后系统宕机，Binlog 没来得及写。</p><ul><li><strong>结果</strong>：重启后，主库通过 Redolog 恢复了这行数据。但因为 Binlog 缺失，从库永远收不到这条更新。</li><li><strong>判定</strong>：<strong>主从不一致</strong>（主库多，从库少）。</li></ul></li><li><p><strong>场景 B（先写 Binlog，后写 Redolog）</strong>：Binlog 写完后系统宕机，Redolog 没来得及写。</p><ul><li><strong>结果</strong>：重启后，主库因为 Redolog 缺失，事务回滚（数据消失）。但从库收到了 Binlog，执行了更新。</li><li><strong>判定</strong>：<strong>主从不一致</strong>（主库少，从库多）。</li></ul></li></ul><p>为了解决这个问题，MySQL 引入了内部 XA 事务机制，也就是 <strong>两阶段提交 (Two-Phase Commit, 2PC)</strong> 机制。</p><p>这是一种严密的协作协议，将事务提交拆解为两个阶段：</p><ol><li><p><strong>Prepare 阶段</strong>：</p><ol><li>InnoDB 将事务的 Redo Log 写入 <strong>Redo Log Buffer</strong>，并根据 <code>innodb_flush_log_at_trx_commit</code> 策略执行 <code>fsync</code> 刷入磁盘。</li><li>InnoDB 将 Redo Log 中该事务的状态标记为 <code>PREPARE</code>，并记录全局事务 ID（XID）。</li><li>此时引擎层通知 Server 层：“数据已持久化，随时待命，可提交也可回滚。”</li></ol></li><li><p><strong>Commit 阶段</strong>：</p><ol><li>Server 层将 Binlog 写入 <strong>Binlog Cache</strong>，并根据 <code>sync_binlog</code> 策略执行 <code>fsync</code> 刷入磁盘。<ul><li>Binlog 的成功落盘是事务成功的唯一“逻辑分水岭”。<strong>一旦 Binlog 成功落盘，此时事务在逻辑上已经成功。</strong></li></ul></li><li>Server 层调用 InnoDB 接口通知提交。InnoDB 将 Redo Log 状态置为 <code>COMMIT</code><ul><li>这一步通常仅需内存操作，无需强制刷盘。因为只要 Binlog 在，崩溃恢复时就能推导出该事务有效。</li></ul></li></ol></li></ol><p>此时我们来看看 2PC 是怎么保证数据一致性的同时完成崩溃恢复的。</p><p>假设服务器在 COMMIT 刚发出或刚写完时突然断电，重启时，InnoDB 必须利用日志将数据库状态恢复到断电前的那一瞬间。</p><p><strong>第一步</strong>：InnoDB 启动时，会去读取最近一次的 <strong>Checkpoint LSN</strong>。Checkpoint 保证了在此之前的脏页都已刷盘，因此无需恢复。</p><p><strong>第二步</strong>：然后 InnoDB 从 Checkpoint LSN 开始，<strong>无差别地</strong> 应用所有 Redo Log。此时，Buffer Pool 被严格还原到宕机前一刻的状态（<strong>包含已提交和未提交的事务</strong>）。</p><p><strong>第三步</strong>：此时内存中存在处于 <code>PREPARE</code> 状态的事务，InnoDB 需要决定是 <strong>提交</strong> 还是 <strong>回滚</strong>。 <strong>裁决的最高准则是：Binlog 的完整性。</strong></p><p>那怎么判断一个事务的 Binlog 是否“完整”？<strong>MySQL 依靠的是特定的“结束标记”</strong>。</p><p>在 <code>ROW</code> 格式下，一个完整的事务在 Binlog 物理文件中长这样：</p><ul><li><strong>Header</strong>：<code>GTID_EVENT</code> &#x2F; <code>BEGIN</code></li><li><strong>Body</strong>：<code>TABLE_MAP_EVENT</code>（操作了哪张表）</li><li><strong>Body</strong>：<code>WRITE_ROWS_EVENT</code> &#x2F; <code>UPDATE_ROWS_EVENT</code>（改了哪些行）</li><li><strong>Footer（关键）</strong>：<strong><code>XID_EVENT</code></strong></li></ul><p>MySQL 在扫描 Binlog 时，<strong>只有读到了最后的这个 <code>XID_EVENT</code>（它里面记录了事务的 XID），才认为这个事务是 “完整” 的</strong>。</p><p>InnoDB 提取 Binlog 中所有拥有完整 <code>XID_EVENT</code> 的事务 ID，与 Redo Log 中的 <code>PREPARE</code> 事务进行核对：</p><ul><li><strong>情况 A：Binlog 中存在该 XID</strong><ul><li><strong>场景推演</strong>：宕机发生在 2PC 的第二阶段（Binlog 已落盘，但 InnoDB 还没来得及改写 Commit 状态）。</li><li><strong>结论</strong>：<strong>提交事务</strong>。</li><li><strong>理由</strong>：Binlog 既然已经写入，说明从库可能已经同步了这条数据。为了保证主从一致，主库必须“认账”，将该事务在引擎层补置为 COMMIT 状态。</li></ul></li><li><strong>情况 B：Binlog 中不存在该 XID</strong><ul><li><strong>场景推演</strong>：宕机发生在 2PC 的第一阶段与第二阶段之间（Redo Log 好了，但 Binlog 没写完或没刷盘）。</li><li><strong>结论</strong>：<strong>回滚事务</strong>。</li><li><strong>理由</strong>：Binlog 没写入，意味着这个事务在逻辑上失败了，且从库永远不会收到。为了保证一致性，主库必须利用 Undo Log 撤销该事务在内存中的修改。</li></ul></li></ul><p>从上面这个流程也能看出来，“双 1”配置（<code>innodb_flush_log_at_trx_commit=1</code> 和 <code>sync_binlog=1</code>）是最安全的，但也意味着 <code>COMMIT</code> 时至少有两次 <code>fsync</code>，性能很差。</p><p>MySQL 5.6 引入了 <strong>Binary Log Group Commit (BLGC)</strong> （组提交）机制，将提交过程细分为三个队列阶段，允许多个并发事务“搭便车”：</p><p>也就是多个事务按顺序将自己的 <code>Binlog</code> 写入操作系统的文件缓存。其中一个事务成为组长，“代表”大家，调用一次 <code>fsync</code>，将整组事务刷入磁盘。将 N 次磁盘 I&#x2F;O 合并为 1 次，极大地提升了并发吞吐量。所有“搭车”成功的事务，再按顺序通知 InnoDB 进行引擎层提交。</p><p>组提交机制不仅解决了 2PC 的性能问题，还带来了两个重要的参数权衡：</p><ul><li><code>binlog_group_commit_sync_delay</code>：为了多攒几个事务，故意等待多少微秒。</li><li><code>binlog_group_commit_sync_no_delay_count</code>：为了多攒几个事务，故意等待凑齐多少个事务。</li></ul><p>ok，我们的事务终于成功提交了！终于安全落盘了…吗？</p><h4 id="第-4-步：终极保险（页断裂与双写）"><a href="#第-4-步：终极保险（页断裂与双写）" class="headerlink" title="第 4 步：终极保险（页断裂与双写）"></a>第 4 步：终极保险（页断裂与双写）</h4><p>还有一个更极端、更恐怖的场景：<strong>页断裂</strong>（逻辑意义上的“断裂”）。</p><p>Linux 文件系统页的大小默认是 4KB。而 <strong>MySQL InnoDB 存储引擎的页大小默认是 16KB</strong>。</p><p>MySQL 程序运行在操作系统之上，依赖操作系统的文件系统与磁盘交互。</p><p>所以当 InnoDB 需要将内存中一个 16KB 的脏页刷到磁盘时，操作系统需要执行 <strong>4 次</strong> 4KB 的 I&#x2F;O 写入操作。</p><p>关键问题在于，<strong>这 4 次 I&#x2F;O 操作 并非原子操作</strong>，如果在这期间服务器突然断电或操作系统崩溃，磁盘上的这个 16KB 数据页就损坏了——它既不是修改前旧数据，也不是修改后的新数据，而是一种“半成品”状态，这就是 <strong>部分写</strong> 问题。</p><p>这种“页数据损坏”是致命的，并且 <strong>无法通过 <code>Redo Log</code> 来修复</strong>。</p><p><strong>因为 <code>Redo Log</code> 记录的是“对页做了什么修改”，它的前提是页本身结构是完整的</strong>，如果页都损坏了，重做修改毫无意义。</p><blockquote><p><code>Redo Log</code> 在磁盘上也是按块存储的，每块 <strong>512 字节</strong>。这与磁盘扇区大小一致，因此 <code>Redo Log</code> 的写入通常能保证原子性。</p></blockquote><p>为了解决这个问题，InnoDB 引入了“<strong>双写</strong>”机制。简单来说，就是在把脏页真正写入分散的数据文件之前，先找个地方把它们 <strong>完整地备份</strong> 一遍。</p><p>脏页刷盘的流程变成了三步：</p><ol><li><strong>内存拷贝</strong>：脏页刷盘时，不直接写磁盘。而是先将脏页复制到内存中的 <strong>Doublewrite Buffer</strong> 区域（大小通常为 2MB）。</li><li><strong>顺序写备份</strong>：当内存 DWB 攒满（例如 1MB）后，InnoDB 发起一次系统调用，将这批页 <strong>顺序写入</strong> 到磁盘上的系统表空间（ibdata）或独立的 <code>.dblwr</code> 文件中。</li><li><strong>离散写落盘</strong>：只有在步骤 2 的备份落盘成功（fsync）后，InnoDB 才会根据脏页的表空间 ID 和页号，将其 <strong>随机写入</strong> 到真正的 <code>.ibd</code> 数据文件中。</li></ol><blockquote><p>在 8.0.20 之前，DWB 寄居在 <code>ibdata1</code> 共享表空间中，容易与 Undo Log、数据字典产生 I&#x2F;O 争用。8.0.20 后，DWB 被分离为独立的 <code>.dblwr</code> 文件（由 <code>innodb_doublewrite_dir</code> 控制），显着降低了争用，提升了吞吐量。</p><p>无论在哪，DWB 的磁盘空间大小通常都是 2MB（即 128 个 16KB 的页），且这 2MB 空间在逻辑上（或物理上）分为两个 1MB 的插槽（Slot）。</p><p>在步骤 2 写入一个插槽的同时，内存 DWB 可以使用另一个插槽接收新的脏页，实现并发。</p></blockquote><p>当 InnoDB 重启时，如果发现某个数据页已损坏（页断裂），它会先去 <code>Doublewrite</code> 磁盘区域找到该页的 <strong>完好副本</strong>，用副本覆盖坏页，<em>然后再</em> 开始执行 <code>Redo Log</code> 进行恢复。</p><p>DWB 虽然安全，但带来了一个代价：<strong>写放大</strong>。数据需要写两次磁盘（一次顺序，一次随机），I&#x2F;O 压力翻倍。</p><p>现代的高端 SSD 硬件（如 Fusion-IO）或特定文件系统（如支持原子写的 XFS）可以保证 16KB 写入的原子性。如果底层硬件支持原子写（即保证 16KB 要么全写成功，要么全不写），那么“页断裂”问题就不复存在。在这种环境下，可以将 <code>innodb_doublewrite</code> 设置为 <code>OFF</code>。这将消除 50% 的冗余 I&#x2F;O，大幅提升数据库的写性能。</p><h3 id="Undo-Log-的第二使命——MVCC"><a href="#Undo-Log-的第二使命——MVCC" class="headerlink" title="Undo Log 的第二使命——MVCC"></a><code>Undo Log</code> 的第二使命——MVCC</h3><p>我们的事务终于安全落盘了，但 Undo Log 不会立即被删除，它还有第二个使命。在多用户并发访问数据库的现实世界中，它从“后悔药”摇身一变，成为了 <strong>MVCC (多版本并发控制)</strong> 的基石。</p><p>为了解决“读-写”冲突，避免读取数据时加锁阻塞，MySQL 利用 Undo Log 保留了数据的历史版本：</p><ul><li><strong>写操作</strong>：修改数据时，旧版本并不直接覆盖，而是被移动到 Undo Log 中。</li><li><strong>读操作</strong>：根据事务开启的时间点，去读 Undo Log 里的“历史快照”。</li><li><strong>结果</strong>：<strong>读不阻塞写，写不阻塞读</strong>。</li></ul><p>根据 MVCC 的需求，InnoDB 对 Undo Log 的生命周期管理如下：</p><ul><li><strong>Insert Undo Log</strong>：新插入的数据没有旧版本，事务提交后即可 <strong>立即删除</strong>。</li><li><strong>Update Undo Log</strong>：由于 MVCC 需要利用它构建历史快照，事务提交后 <strong>不能立即删除</strong>。</li><li><strong>Purge 线程</strong>：后台线程会持续监控，只有当确认全系统没有任何事务再需要这些旧版本时，才会真正清理这些日志并释放空间。</li></ul><blockquote><p>本篇不涉及 MVCC 的详细讲解</p></blockquote>]]></content>
    
    
    <categories>
      
      <category>数据库</category>
      
    </categories>
    
    
    <tags>
      
      <tag>InnoDB</tag>
      
      <tag>Buffer Pool</tag>
      
      <tag>性能优化</tag>
      
    </tags>
    
  </entry>
  
  
  
  <entry>
    <title>ThreadLocal</title>
    <link href="/2025/12/13/ThreadLocal/"/>
    <url>/2025/12/13/ThreadLocal/</url>
    
    <content type="html"><![CDATA[<p>ThreadLocal 是 <strong>一个用于存储线程私有变量的工具</strong>，它 <strong>为每个线程维护独立的变量副本</strong>，<strong>实现线程间数据隔离</strong>，避免了多线程并发访问的安全问题；还可在同一线程内完成上下文参数的隐式透传，避免方法间显式传参。</p><p>在并发编程中，我们习惯于讨论如何用“锁”来控制对共享资源的访问。但 <code>ThreadLocal</code> 提供了一种完全不同的思路：<strong>与其争抢，不如隔离</strong>。</p><p><code>ThreadLocal</code> 的 <strong>设计初衷并非为了解决多线程“共享”变量</strong> 的问题，而是为了实现 <strong>线程封闭（Thread Confinement）</strong>。它通过物理隔离，让每个线程拥有独立的变量副本，从根本上切断了竞争的源头，实现了无锁的线程安全。</p><p>它不仅仅是一个存储工具，更是 Java 这种基于线程模型的语言中，处理上下文（Context）和事务（Transaction）的基石。</p><h2 id="两大核心场景"><a href="#两大核心场景" class="headerlink" title="两大核心场景"></a>两大核心场景</h2><p><code>ThreadLocal</code> 在 Java 生态中主要扮演两个角色：<strong>隐式参数通道</strong> 和 <strong>事务管理基石</strong>。</p><h3 id="场景一：全链路参数透传"><a href="#场景一：全链路参数透传" class="headerlink" title="场景一：全链路参数透传"></a>场景一：全链路参数透传</h3><p>假设你正在维护一个复杂的业务链路：$Controller \rightarrow ServiceA \rightarrow ServiceB …… \rightarrow Dao$</p><p>现在的需求是：在数据库新增一个字段 <code>modified_by</code>（修改人），需要从 Controller 层获取当前登录用户的 ID，一直透传到 Dao 层进行存储。</p><ul><li><strong>笨办法（显式传参）</strong>：你需要修改链路上所有方法的签名，把 <code>userId</code> 一层层传下去。哪怕中间的 <code>Service B</code> 根本不需要它，也必须被迫充当“搬运工”。代码侵入性极强。</li><li><strong>错办法（静态变量）</strong>：搞一个 <code>public static String currentUserId</code>？绝对不行。Web 服务器是多线程的，线程 A 刚赋值“张三”，线程 B 可能瞬间把它改成了“李四”，数据全乱套。</li></ul><p>解法：<strong>ThreadLocal 隐式通道</strong></p><p>基于 Web 容器（如 Tomcat 或 Jetty）的 <strong>Thread-Per-Request</strong> 模型（一个请求对应一个独立线程），我们可以利用 <code>ThreadLocal</code> 将数据绑定到当前线程上。</p><figure class="highlight java"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br></pre></td><td class="code"><pre><code class="hljs java"><span class="hljs-keyword">public</span> <span class="hljs-keyword">class</span> <span class="hljs-title class_">UserContext</span> &#123;<br>    <span class="hljs-keyword">private</span> <span class="hljs-keyword">static</span> <span class="hljs-keyword">final</span> ThreadLocal&lt;String&gt; USER_HOLDER = <span class="hljs-keyword">new</span> <span class="hljs-title class_">ThreadLocal</span>&lt;&gt;();<br><br>    <span class="hljs-keyword">public</span> <span class="hljs-keyword">static</span> <span class="hljs-keyword">void</span> <span class="hljs-title function_">set</span><span class="hljs-params">(String userId)</span> &#123;<br>        USER_HOLDER.set(userId);<br>    &#125;<br><br>    <span class="hljs-keyword">public</span> <span class="hljs-keyword">static</span> String <span class="hljs-title function_">get</span><span class="hljs-params">()</span> &#123;<br>        <span class="hljs-keyword">return</span> USER_HOLDER.get();<br>    &#125;<br>    <br>    <span class="hljs-keyword">public</span> <span class="hljs-keyword">static</span> <span class="hljs-keyword">void</span> <span class="hljs-title function_">remove</span><span class="hljs-params">()</span> &#123;<br>        USER_HOLDER.remove(); <span class="hljs-comment">// 必须清理，防止内存泄漏</span><br>    &#125;<br>&#125;<br></code></pre></td></tr></table></figure><p>有人可能会问：<code>USER_HOLDER</code> 是 <code>static</code> 的，全局只有一份，怎么能存不同线程的数据？</p><p>这跟 ThreadLocal 的底层设计有关，下面讲源码时会讲到：因为 <strong>ThreadLocal 仅仅是一个访问入口 (Key) 和管理工具，真正存储数据 (Value) 的容器是 <code>Thread</code> 线程对象自身内部的 <code>ThreadLocalMap</code></strong>。</p><h3 id="场景二：数据库连接与事务管理"><a href="#场景二：数据库连接与事务管理" class="headerlink" title="场景二：数据库连接与事务管理"></a>场景二：数据库连接与事务管理</h3><p>如果说参数传递只是“便利性”问题，那么在数据库连接管理上，<code>ThreadLocal</code> 解决的是生与死的“正确性”问题。</p><p>JDBC 的 <code>java.sql.Connection</code> 是 <strong>非线程安全</strong> 的。</p><p>假设我们要执行更新：</p><ol><li><strong>prepareStatement</strong>：创建 SQL 模板（<code>UPDATE account SET balance = ?</code>）。</li><li><strong>Statement</strong>：这是一个“占位符容器”，是执行 SQL 的骨架。</li><li><strong>executeUpdate</strong>：传入参数，执行。</li></ol><p>假设我们为了省事，创建一个全局共享的 <code>static Connection conn</code>。</p><ul><li><strong>T1 (线程 A)</strong>：拿到 <code>conn</code>，创建 Statement，设置参数 <code>balance = -100</code>。</li><li><strong>T2 (线程 B)</strong>：拿到同一个 <code>conn</code>，创建 Statement，设置参数 <code>balance = +100</code>。</li><li><strong>竞态条件</strong>：<code>Connection</code> 内部通常维护一个当前的 <code>Statement</code> 引用或缓冲区。T2 的操作可能会覆盖 T1 的上下文。</li><li><strong>结果</strong>：T1 执行提交时，可能把 T2 的数据提交了，或者抛出异常。</li></ul><p>失败的尝试</p><ul><li><p><strong>方案 A：加锁 (Synchronized)</strong></p><ul><li>做法：所有数据库操作都加锁。</li><li><strong>代价</strong>：系统吞吐量雪崩。如果有 1000 个并发请求，它们将排队等待这一个连接。这退化成了单线程应用。</li></ul></li><li><p><strong>方案 B：多连接 + 复杂调度</strong></p><ul><li>做法：手动写代码管理连接池，谁用谁取。</li><li><strong>死锁风险</strong>：如果通过连接 A 操作库 1，连接 B 操作库 2。线程 1 拿了 A 等 B，线程 2 拿了 B 等 A，系统直接死锁。</li></ul></li></ul><p>解法：<strong>连接复用与隔离</strong></p><p>这是 Spring <code>@Transactional</code> 声明式事务的魔法来源。</p><p>Spring 的事务管理器（TransactionManager）利用 <code>ThreadLocal</code> 确保：<strong>同一个线程，在同一个事务范围内，使用的是同一个数据库连接</strong>。</p><p><strong>内部核心类 <code>TransactionSynchronizationManager</code></strong> 大致逻辑如下：</p><figure class="highlight java"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br></pre></td><td class="code"><pre><code class="hljs java"><span class="hljs-comment">// 伪代码：Spring 事务管理的资源持有者</span><br><span class="hljs-keyword">private</span> <span class="hljs-keyword">static</span> <span class="hljs-keyword">final</span> ThreadLocal&lt;Map&lt;Object, Object&gt;&gt; resources = <span class="hljs-keyword">new</span> <span class="hljs-title class_">ThreadLocal</span>&lt;&gt;();<br></code></pre></td></tr></table></figure><ul><li><code>ThreadLocal&lt;Map&lt;Object, Object&gt;&gt;</code>：<strong>最核心</strong>。Key 是 DataSource，Value 是 ConnectionHolder（包装了 JDBC Connection）。确保同一线程、同一事务内复用同一个连接。</li><li><code>ThreadLocal&lt;Set&lt;...&gt;&gt;</code>：存储当前线程的事务回调（如 commit 后发消息、清理缓存）。</li><li><code>ThreadLocal&lt;String&gt;</code>：存储当前事务名称，用于监控和日志。</li></ul><p>执行流程 (DataSourceTransactionManager)：</p><ol><li><strong>开启事务</strong>：DSTM 从连接池获取一个全新的 <code>Connection</code>。</li><li><strong>绑定资源</strong>：调用 <code>TSM.bindResource()</code>，将这个 <code>Connection</code> 放入当前线程的 <code>ThreadLocal (resources)</code> 中。</li><li><strong>业务执行</strong>：Dao 层执行 SQL 时，不是自己去连接池拿连接，而是通过 <code>DataSourceUtils</code> 从 <code>ThreadLocal</code> 中获取当前事务绑定的那个连接。这样，虽然 ServiceA、ServiceB、ServiceC 在不同方法，但因为在同一个线程，拿到的都是 <strong>同一个 <code>Connection</code></strong>。这保证了它们在同一个事务内。</li><li><strong>提交&#x2F;回滚</strong>：利用该连接进行 commit&#x2F;rollback。</li><li><strong>清理</strong>：调用 <code>TSM.unbindResource()</code>，清空 <code>ThreadLocal</code>，将连接归还给连接池。</li></ol><p>这不仅避免了昂贵的锁竞争，还完美解决了跨方法的事务传播问题。</p><h2 id="核心原理：Thread-才是真正的仓库"><a href="#核心原理：Thread-才是真正的仓库" class="headerlink" title="核心原理：Thread 才是真正的仓库"></a>核心原理：Thread 才是真正的仓库</h2><p><strong>误区</strong>：认为 <code>ThreadLocal</code> 内部维护了一个 <code>Map&lt;Thread, Object&gt;</code>，把数据存得满地都是。</p><p><strong>真相</strong>：<strong>数据其实是存在线程（Thread）自己的“口袋”里的</strong>。</p><h3 id="内存关系图"><a href="#内存关系图" class="headerlink" title="内存关系图"></a>内存关系图</h3><ul><li><strong>Thread (宿主)</strong>：每个 <code>Thread</code> 对象内部都有一个 <code>ThreadLocalMap</code> 类型的成员变量 <code>threadLocals</code>。</li><li><strong>ThreadLocalMap (口袋)</strong>：这是 <code>ThreadLocal</code> 的静态内部类，本质是一个定制版的 HashMap。</li><li><strong>ThreadLocal (钥匙)</strong>：它只是一个访问入口。</li></ul><figure class="highlight java"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br></pre></td><td class="code"><pre><code class="hljs java">Thread (线程对象)<br>  |<br>  |__ <span class="hljs-title function_">threadLocals</span> <span class="hljs-params">(成员变量, 类型为 ThreadLocalMap)</span><br>        |<br>        |__ Entry[] (数组, 存储多个 Entry)<br>              |<br>              |__ Entry <span class="hljs-number">1</span><br>              |     |__ <span class="hljs-title function_">Key</span>   <span class="hljs-params">(弱引用指向 ThreadLocal A)</span><br>              |     |__ <span class="hljs-title function_">Value</span> <span class="hljs-params">(强引用指向 业务数据 A)</span><br>              |<br>              |__ Entry <span class="hljs-number">2</span><br>                    |__ <span class="hljs-title function_">Key</span>   <span class="hljs-params">(弱引用指向 ThreadLocal B)</span><br>                    |__ <span class="hljs-title function_">Value</span> <span class="hljs-params">(强引用指向 业务数据 B)</span><br></code></pre></td></tr></table></figure><figure class="highlight java"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br><span class="line">17</span><br><span class="line">18</span><br></pre></td><td class="code"><pre><code class="hljs java"><span class="hljs-keyword">public</span> <span class="hljs-keyword">class</span> <span class="hljs-title class_">Thread</span> &#123;<br>    <span class="hljs-comment">// 真正的仓库在 Thread 类里面</span><br>    ThreadLocal.<span class="hljs-type">ThreadLocalMap</span> <span class="hljs-variable">threadLocals</span> <span class="hljs-operator">=</span> <span class="hljs-literal">null</span>;<br>&#125;<br><br><span class="hljs-keyword">public</span> <span class="hljs-keyword">class</span> <span class="hljs-title class_">ThreadLocal</span>&lt;T&gt; &#123;<br>    <span class="hljs-keyword">public</span> <span class="hljs-keyword">void</span> <span class="hljs-title function_">set</span><span class="hljs-params">(T value)</span> &#123;<br>        <span class="hljs-comment">// 获取当前线程</span><br>        <span class="hljs-type">Thread</span> <span class="hljs-variable">t</span> <span class="hljs-operator">=</span> Thread.currentThread();<br>        <span class="hljs-comment">// 拿到线程自己的 Map</span><br>        <span class="hljs-type">ThreadLocalMap</span> <span class="hljs-variable">map</span> <span class="hljs-operator">=</span> getMap(t);<br>        <span class="hljs-keyword">if</span> (map != <span class="hljs-literal">null</span>)<br>            <span class="hljs-comment">// 【关键】以 &quot;this&quot; (ThreadLocal实例) 为 Key，存入数据</span><br>            map.set(<span class="hljs-built_in">this</span>, value);<br>        <span class="hljs-keyword">else</span><br>            createMap(t, value);<br>    &#125;<br>&#125;<br></code></pre></td></tr></table></figure><h3 id="为什么-ThreadLocalMap-不用链表？"><a href="#为什么-ThreadLocalMap-不用链表？" class="headerlink" title="为什么 ThreadLocalMap 不用链表？"></a>为什么 ThreadLocalMap 不用链表？</h3><p><code>ThreadLocalMap</code> 是 <code>ThreadLocal</code> 的静态内部类，虽然名为 Map，但它与 <code>HashMap</code> 有显著不同：</p><ul><li><strong>没有链表&#x2F;红黑树</strong>：它完全采用数组存储 (<code>Entry[] table</code>)。</li><li><strong>解决冲突策略</strong>：采用 <strong>开放寻址法</strong> 中的 <strong>线性探测</strong>。计算 hash 对应下标，如果该位置已被占用，则 <code>index + 1</code> 向后寻找下一个空位，直到找到为止。</li><li><strong>优势</strong>：结构简单，<strong>省内存</strong>（无链表指针开销）。数组内存连续，利用 <strong>CPU 缓存行机制</strong>，在冲突较少时访问效率极高。</li><li><strong>劣势</strong>：如果冲突严重，探测路径变长，性能会急剧下降。这也是为什么建议 ThreadLocal 存储对象不宜过多。<ul><li><strong>应对</strong>：JDK 引入了 <strong>黄金分割数魔数</strong> <code>0x61c88647</code>（与 <strong>黄金分割点</strong> 有关。$ ( \sqrt{5} - 1 ) &#x2F; 2 \times 2^{32} \approx 2654435769 $ (即 0x61c88647)）。每创建一个 ThreadLocal，哈希值就累加这个数。实验证明，这能让数据在 $2^n$ 大小的数组中分布得极其均匀，最大程度减少冲突。对使用“线性探测法”的 Map 至关重要。</li></ul></li></ul><h2 id="隐患：内存泄漏"><a href="#隐患：内存泄漏" class="headerlink" title="隐患：内存泄漏"></a>隐患：内存泄漏</h2><p>既然数据存在线程里，而线程（特别是线程池核心线程）是长期存活的，那这些数据岂不是永远无法释放？</p><p>先来看看这个情况：</p><p>如果我们新建了一个线程，这个线程做完自己的事，然后被销毁：</p><p>Thread 创建 ThreadLocal 并调用 set 方法。创建 ThreadLocalMap，并与之绑定。</p><p>现在的引用关系：</p><ul><li>Thread 引用 ThreadLocalMap。</li><li>Enrty 的 key 又存储了 ThreadLocal 的引用，value 存储了我们 set 的对象的引用。</li></ul><p>Thread 的任务执行完毕，Thread 被销毁：</p><ul><li>此时 Thread 对 ThreadLocalMap 的引用消失。ThreadLocalMap 当前没有任何引用，下一次 GC 被回收。</li><li>此时 Enrty 数组也失去了引用，下一次 GC 被回收。</li><li>Enrty 中的 key（ThreadLocal），value 不再被引用，GC 回收。</li></ul><p>上面是一个线程正常的生命流程，没有任何的内存泄漏。</p><p>但如果这个线程是 <strong>线程池的核心线程</strong>，十天半个月甚至几个月都不销毁，那么 Thread 就会一直引用着 ThreadLocalMap，进而导致 key 和 value 一直不能被回收。这样就发生了内存泄漏。</p><p>为了缓解这个问题，JDK 采用了一种“<strong>弱引用</strong>”策略。但这只是 <strong>半个</strong> 解法。</p><p><code>ThreadLocalMap</code> 的 Entry 定义如下：</p><figure class="highlight java"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br></pre></td><td class="code"><pre><code class="hljs java"><span class="hljs-comment">// Entry 继承自 WeakReference，Key 是弱引用</span><br><span class="hljs-keyword">static</span> <span class="hljs-keyword">class</span> <span class="hljs-title class_">Entry</span> <span class="hljs-keyword">extends</span> <span class="hljs-title class_">WeakReference</span>&lt;ThreadLocal&lt;?&gt;&gt; &#123;<br>    Object value; <span class="hljs-comment">// Value 是强引用</span><br>    Entry(ThreadLocal&lt;?&gt; k, Object v) &#123;<br>        <span class="hljs-built_in">super</span>(k); <br>        value = v;<br>    &#125;<br>&#125;<br></code></pre></td></tr></table></figure><ul><li><strong>Key (ThreadLocal)</strong>：<strong>若 Key 为强引用</strong>，只要线程存活（如线程池核心线程），Map 就持有 Key 的引用。即使外部局部变量 <code>tl</code> 销毁，<code>ThreadLocal</code> 对象也无法回收，导致必然的内存泄漏。<strong>改为弱引用后</strong>，外部强引用消失，GC 会回收 <code>ThreadLocal</code> 对象，Map 中的 Key 变为 <code>null</code>。</li><li><strong>Value (业务数据)</strong>：<strong>强引用</strong>。必须强引用，Value 是业务数据，其 <strong>生命周期必须由业务代码控制</strong>。若为弱引用，可能在业务逻辑执行中途被 GC 清理，导致 <code>NullPointerException</code>。</li></ul><figure class="highlight java"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br><span class="line">17</span><br><span class="line">18</span><br><span class="line">19</span><br><span class="line">20</span><br><span class="line">21</span><br><span class="line">22</span><br></pre></td><td class="code"><pre><code class="hljs java">[当前线程栈帧]                [堆内存 Heap]<br>+-------------+            +---------------------+<br>| 正在执行的方法 |            |  Thread 对象 (张三)  |<br>|             |            |                     |<br>| 局部变量 tl  |-----------&gt;|  threadLocals (Map) |<br>+------+------+            |   (张三的口袋)        |<br>       |                   |   +-------------+   |<br>       | (强引用)           |   | Entry[]数组 |   |<br>       |                   |   +------+------+   |<br>       |                   +----------|----------+<br>       |                              |<br>       |                              |<br>       v                              v<br>+----------------+         +------------------+<br>| ThreadLocal对象 | &lt;-------| Entry       |<br>|   (钥匙)        | (弱引用) | Key      Value   |<br>+----------------+         +-----------+------+<br>                                       |(强引用) <br>                                       v<br>                                 +------------+<br>                                 | 业务数据Obj |<br>                                 +------------+<br></code></pre></td></tr></table></figure><p>所以，value 怎么办呢？当 Key 被 GC 回收变成 <code>null</code> 后，引用链就变成了：</p><blockquote><p><strong>Current Thread</strong>-&gt; <strong>ThreadLocalMap</strong>-&gt; <strong>Entry</strong>-&gt; <strong>Value</strong>-&gt; <strong>Object</strong></p></blockquote><ul><li><strong>问题</strong>：Key 没了，你再也访问不到这个 Value 了。</li><li><strong>后果</strong>：只要线程不结束，这条强引用链就一直存在，这块内存永远无法回收。这就是内存泄漏。</li></ul><p>虽然 <code>ThreadLocalMap</code> 采用了一种“探测式清理”策略。在调用 <code>set()</code>、<code>get()</code>、<code>remove()</code> 时，它会顺便检查哈希槽里有没有 Key 为 <code>null</code> 的脏 Entry，如果有就清理掉。但这种清理是被动的 <strong>。一旦你不再调用这些方法，或者线程归还给线程池后处于空闲状态，垃圾就会一直堆积</strong>。</p><p>设为 static 就不会有内存泄漏的问题了？</p><p>网上会有很多教程说，设置为 static 就不会有内存泄漏问题了。那我们来看看吧。就拿一开始的应用举例。</p><p>设置为 static，全局将只有这一个 ThreadLocal。所有线程共用这一个静态的 ThreadLocal。当然，这不影响线程隔离，因为实际上实现线程隔离的是 ThreadLocalMap。</p><p>这种解决方案是，全局仅使用一个 ThreadLocal 作为一把“钥匙”。第一次请求存入了 ValueA。这条请求即使结束了，我们依旧可以用 ThreadLocal.get 定位到这个对象。因为 ThreadLocal 是全局唯一的。</p><p>而下一次，有新的请求，又调用了 set 方法，就会直接覆盖掉上一次 set 的对象。</p><p>这样子大概已经够用了。但还是会有问题。假如我们存入了一个非常大的对象，又一直没有新的请求调用 ThreadLocal.set，这个对象就会一直放在这里，浪费内存。</p><h2 id="最佳实践"><a href="#最佳实践" class="headerlink" title="最佳实践"></a>最佳实践</h2><h3 id="为什么-Map-存在于-Thread-对象中，而不是-ThreadLocal-对象中？"><a href="#为什么-Map-存在于-Thread-对象中，而不是-ThreadLocal-对象中？" class="headerlink" title="为什么 Map 存在于 Thread 对象中，而不是 ThreadLocal 对象中？"></a>为什么 Map 存在于 Thread 对象中，而不是 ThreadLocal 对象中？</h3><p>假设 <code>ThreadLocal</code> 自己维护一个 Map，用来记录各个线程存了什么值。</p><p>结构大概是：<code>Map&lt;Thread, Value&gt;</code>。</p><ul><li><p><strong>逻辑：</strong> 线程 A 来存数据，<code>ThreadLocal</code> 就记录 <code>map.put(ThreadA, &quot;数据&quot;)</code>。</p></li><li><p><strong>致命缺陷 1：并发冲突（性能差）</strong></p><ul><li><code>ThreadLocal</code> 对象通常是全局共享的（比如定义为 <code>static final</code>）。</li><li>如果有 100 个线程同时在这个 <code>ThreadLocal</code> 中存取数据，它们访问的是 <strong>同一个 Map</strong>。</li><li>为了数据安全，这个 Map 必须加锁（或者用 <code>ConcurrentHashMap</code>）。<strong>这会导致严重的锁竞争</strong>，违背了 ThreadLocal “线程隔离”的初衷。</li></ul></li><li><p><strong>致命缺陷 2：生命周期不一致（内存泄漏）</strong></p><ul><li>如果线程 A 销毁了，但 <code>ThreadLocal</code> 里的 Map 还存着 <code>Key=ThreadA</code> 的数据。你必须有一种机制去监听线程销毁事件来清理 Map，否则全是垃圾数据。</li></ul></li></ul><p>现在的结构是：<code>Thread</code> 对象内部有一个成员变量 <code>ThreadLocalMap threadLocals</code>。</p><p>这个 Map 的 Key 是 <code>ThreadLocal</code> 对象，Value 是具体的值。</p><ul><li><p><strong>逻辑：</strong></p><ul><li><code>ThreadLocal</code> 更像是一个 <strong>“访问入口”</strong> 或 <strong>“钥匙”</strong>。</li><li>当你调用 <code>tl.set(&quot;数据&quot;)</code> 时，<code>ThreadLocal</code> 实际上是转身去找当前线程：“嘿，线程老兄，把你口袋里的 Map 拿出来，把‘我’作为 Key，‘数据’作为 Value 存进去。”</li><li>代码逻辑：<code>Thread.currentThread().map.put(this, &quot;数据&quot;)</code>。</li></ul></li><li><p><strong>优势 1：绝对的线程安全（无锁）</strong></p><ul><li>每个线程只访问 <strong>自己的</strong> Map（存在自己的 Thread 对象里）。</li><li>线程 A 访问线程 A 的 Map，线程 B 访问线程 B 的 Map。</li><li><strong>完全不存在竞争，不需要任何锁</strong>。这就是 ThreadLocal 高效的根本原因。</li></ul></li><li><p><strong>优势 2：生命周期自动绑定</strong></p><ul><li>数据是存在 <code>Thread</code> 对象里的。</li><li><strong>一旦线程销毁</strong>（Thread 对象被 GC），它肚子里的 <code>ThreadLocalMap</code> 自然也就销毁了，里面的 Value 也就随之释放了。</li><li>（注：这指的是线程真正销毁的情况。如果是线程池，线程不死，Map 不毁，所以才需要弱引用机制来辅助回收 Key）。</li></ul></li></ul><h3 id="铁律：Try-Finally-必须-Remove"><a href="#铁律：Try-Finally-必须-Remove" class="headerlink" title="铁律：Try-Finally 必须 Remove"></a>铁律：Try-Finally 必须 Remove</h3><p>这是防止内存泄漏的唯一绝对手段，没有之一。</p><figure class="highlight java"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br></pre></td><td class="code"><pre><code class="hljs java"><span class="hljs-keyword">try</span> &#123;<br>    UserContext.set(user);<br>    <span class="hljs-comment">// 执行业务逻辑</span><br>    doSomething();<br>&#125; <span class="hljs-keyword">finally</span> &#123;<br>    <span class="hljs-comment">// 必须在 finally 中清理，确保发生异常也能执行</span><br>    UserContext.remove();<br>&#125;<br></code></pre></td></tr></table></figure><h3 id="如何解决父子线程传递？"><a href="#如何解决父子线程传递？" class="headerlink" title="如何解决父子线程传递？"></a>如何解决父子线程传递？</h3><p><strong>问题</strong>：<code>ThreadLocal</code> 里的数据，子线程（<code>new Thread</code>）是拿不到的。</p><p><strong>原生方案</strong>：<code>InheritableThreadLocal</code>。它在创建线程时，会将父线程的 Map <strong>浅拷贝</strong> 给子线程。</p><ul><li><strong>致命缺陷</strong>：它只在“创建线程”那一刻拷贝。但在 <strong>线程池</strong> 场景下，线程是复用的，不会重复触发创建过程。后续提交的任务无法获取父线程最新的上下文。</li></ul><p><strong>工业级解法</strong>：使用阿里开源的 <strong>TTL (TransmittableThreadLocal)</strong>。它通过 <strong>装饰器模式</strong>，对 <code>Runnable</code>&#x2F;<code>Callable</code> 进行修饰，在任务提交和执行的时机进行动态挂载和恢复上下文。</p>]]></content>
    
    
    <categories>
      
      <category>并发编程</category>
      
    </categories>
    
    
    <tags>
      
      <tag>ThreadLocal</tag>
      
      <tag>线程安全</tag>
      
      <tag>上下文传递</tag>
      
    </tags>
    
  </entry>
  
  
  
  <entry>
    <title>分布式事务</title>
    <link href="/2025/11/22/%E5%88%86%E5%B8%83%E5%BC%8F%E4%BA%8B%E5%8A%A1/"/>
    <url>/2025/11/22/%E5%88%86%E5%B8%83%E5%BC%8F%E4%BA%8B%E5%8A%A1/</url>
    
    <content type="html"><![CDATA[<p>在单体应用时代，事务（Transaction）是数据库送给开发者最昂贵的礼物。你只需要由 Spring 加上 <code>@Transactional</code> 注解，数据库就会保证：要么全做，要么全不做。</p><p>但当你把系统从单体拆分成微服务，或者引入了多个数据库时，这个“魔法”失效了。</p><p>假设你在开发一个电商系统。用户点击“下单”按钮，后台需要发生两个动作：</p><ol><li><strong>订单服务</strong>：在订单库创建一条订单记录。</li><li><strong>库存服务</strong>：在库存库扣减一个商品库存。</li></ol><p>如果在单体架构下，这是一个本地事务。但在微服务架构下，这是两个跨网络的独立调用。如果订单创建成功了，网络抖动导致库存扣减请求超时，或者库存扣减成功了，但订单服务挂了，数据库的数据就会出现“不一致”：多卖了货，或者少记了账。</p><p>这就是分布式事务要解决的核心问题：<strong>如何跨越网络的不确定性，维持数据的一致性</strong>？</p><h2 id="理论基石"><a href="#理论基石" class="headerlink" title="理论基石"></a>理论基石</h2><p>在深入具体方案之前，我们需要理解悬在所有分布式系统头顶的两把利剑：<strong>CAP 定理</strong> 与 <strong>BASE 理论</strong>。</p><h3 id="CAP-定理：不得不做的选择题"><a href="#CAP-定理：不得不做的选择题" class="headerlink" title="CAP 定理：不得不做的选择题"></a>CAP 定理：不得不做的选择题</h3><p>该理论最初由 Eric Brewer 在 2000 年 PODC 会议上提出猜想，后由 Seth Gilbert 和 Nancy Lynch 在 2002 年通过形式化证明确立。</p><p>定理内容为：在一个分布式数据存储系统中，<strong>一致性、可用性、分区容错性</strong> 这三个基本要素，<strong>最多只能同时满足其中的两项</strong>。</p><ul><li>C (<strong>C</strong> onsistency)：一致性（所有节点在同一时间看到的数据是相同的）。</li><li>A (<strong>A</strong> vailability)：可用性（系统总能响应请求，即使部分节点挂了）。</li><li>P (<strong>P</strong> artition tolerance)：分区容错性（当 <strong>网络因为故障导致节点之间通信中断（即发生网络分区）</strong>，系统仍能持续运行并提供服务。）。</li></ul><p>由于 <strong>网络不可靠是客观事实</strong>，<strong>P（分区容错）是必须满足的前提</strong>。</p><p>因此，真正的抉择在于：当网络分区发生时，系统选择 CP（强一致，但在网络抖动时系统不可用） 还是 AP（高可用，但允许数据暂时不一致）。</p><p>CP 架构：一致性优先</p><ul><li><strong>设计逻辑</strong>：当发生分区导致部分节点无法同步时，为了保证数据准确（C），系统选择拒绝服务或阻塞请求，直到网络恢复并达成共识。</li><li><strong>代价</strong>：在分区期间，部分或全部服务不可用，系统的可用性下降。</li><li><strong>核心技术</strong>：<ul><li><strong>共识算法</strong>：Paxos, Raft, Zab。</li><li><strong>多数派机制（Quorum）</strong>：写入必须获得超过半数节点的确认。</li></ul></li><li><strong>典型系统</strong>：<ul><li><strong>ZooKeeper &#x2F; Etcd</strong>：用于分布式协调和元数据管理，Leader 选举期间集群对外不可用。</li><li><strong>HBase</strong>：基于 RegionServer 的行锁机制保证强一致性。</li><li><strong>Redis (Sentinel&#x2F;Cluster)</strong>：在默认配置及故障转移期间，实际上倾向于 CP，但在某些极端情况下可能丢失数据（异步复制导致）。</li></ul></li><li><strong>适用场景</strong>：金融交易、库存扣减、分布式锁、元数据管理。</li></ul><p>AP 架构：可用性优先</p><ul><li><strong>设计逻辑</strong>：当发生分区时，优先保证业务不中断，允许节点返回本地的旧版本数据（Stale Data）。待网络恢复后，再通过后台机制进行数据合并与冲突解决。</li><li><strong>代价</strong>：不同分区的节点持有不同版本的数据，出现“脑裂”或数据不一致，需要复杂的冲突合并逻辑。</li><li><strong>核心技术</strong>：<ul><li><strong>Gossip 协议</strong>：节点间随机通信传播状态。</li><li><strong>冲突解决</strong>：向量时钟（Vector Clock）、最后写入胜出（LWW）、CRDTs（无冲突复制数据类型）。</li><li><strong>反熵机制（Anti-Entropy）</strong>：如 Read Repair（读修复）和 Hinted Handoff（提示移交）。</li></ul></li><li><strong>典型系统</strong>：<ul><li><strong>Cassandra &#x2F; DynamoDB</strong>：基于最终一致性模型，写入即可返回，后台异步同步。</li><li><strong>Eureka</strong>：服务发现组件，优先保证客户端能获取服务列表，即使列表包含已下线的服务。</li></ul></li><li><strong>适用场景</strong>：社交动态点赞、商品评论、DNS 解析、Web 缓存、内容分发网络（CDN）。</li></ul><blockquote><p>PACELC 理论</p><p>由 Daniel Abadi 提出，填补了 CAP 理论中“无分区时如何权衡”的空白。</p><ul><li><strong>P (Partition)</strong>：如果有网络分区，必须在 <strong>A（可用性）</strong> 和 <strong>C（一致性）</strong> 之间选择。</li><li><strong>E (Else)</strong>：如果没有网络分区（系统正常运行时），必须在 <strong>L（Latency，延迟）</strong> 和 <strong>C（一致性）</strong> 之间选择。</li><li><strong>深层含义</strong>：强一致性通常意味着需要等待更多节点的确认，这必然增加延迟。如果追求极致的低延迟，往往需要放宽一致性要求。</li></ul></blockquote><h3 id="BASE-理论：反抗强一致性"><a href="#BASE-理论：反抗强一致性" class="headerlink" title="BASE 理论：反抗强一致性"></a>BASE 理论：反抗强一致性</h3><p>为了追求互联网应用的高可用，我们往往退而求其次，遵循 <strong>BASE</strong> 理论：</p><ul><li><strong>BA (Basically Available)</strong>：基本可用（允许服务降级）。</li><li><strong>S (Soft state)</strong>：软状态（允许存在中间状态，如“支付中”）。</li><li><strong>E (Eventually consistent)</strong>：最终一致性（经过一段时间，数据终变一致）。</li></ul><p>反面教材：2PC (两阶段提交)</p><p>最早的尝试是 <strong>2PC (Two-Phase Commit)</strong>，这是一种试图在分布式环境实现“严格原子性”的协议。</p><ul><li><strong>阶段 1 (Prepare)</strong>：协调者问所有参与者：“能提交吗？”参与者锁定资源，写日志，但不提交。</li><li><strong>阶段 2 (Commit)</strong>：全员回复 Yes 则提交；只要有一个 No 则全员回滚。</li></ul><p>为什么互联网大厂不用它？</p><ul><li><strong>同步阻塞</strong>：所有人都要等最慢的那个人。</li><li><strong>死锁风险</strong>：协调者挂了，参与者会无限期持有锁。</li><li><strong>性能毒药</strong>：在微服务高并发场景下，这种长时锁是吞吐量的杀手。</li></ul><h2 id="主流解决方案"><a href="#主流解决方案" class="headerlink" title="主流解决方案"></a>主流解决方案</h2><p><strong>Seata (Simple Extensible Autonomous Transaction Architecture)</strong> 是阿里巴巴开源的一站式分布式事务解决方案，它致力于让分布式事务的使用像本地事务一样简单。</p><h3 id="Seata-AT-模式"><a href="#Seata-AT-模式" class="headerlink" title="Seata AT 模式"></a>Seata AT 模式</h3><p>Seata AT 模式的设计目标是：<strong>把分布式事务变得像本地事务一样简单。</strong></p><p>想象你在编辑文档，当你犯错时，按下 <code>Ctrl+Z</code> 就能撤销。Seata AT 就是给数据库操作装了一个自动的 <code>Ctrl+Z</code>。</p><p>你不需要修改业务代码，只需要在方法上打一个注解。Seata 会利用代理机制，拦截你的 SQL，自动记录“修改前的数据”和“修改后的数据”。如果所有服务都成功，它就清理掉这些记录；如果有服务失败，它就利用这些记录把数据还原。</p><p>Seata 在你的业务 SQL 执行前后，悄悄做了手脚。</p><p>核心流程（伪代码）：</p><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br></pre></td><td class="code"><pre><code class="hljs text">// 阶段一：执行并记录<br>1. 解析业务 SQL： UPDATE product SET stock = 99 WHERE id = 1;<br>2. 查询前镜像 (Before Image)：记录 stock = 100;<br>3. 执行业务 SQL： stock 变为 99;<br>4. 查询后镜像 (After Image)：记录 stock = 99;<br>5. 插入 Undo Log：将前后镜像打包存入 undo_log 表;<br>6. 提交本地事务 (业务数据 + undo_log 同时生效);<br><br>// 阶段二：决断<br>IF (全局提交) &#123;<br>    异步删除 undo_log; // 极其快速<br>&#125; ELSE (全局回滚) &#123;<br>    根据 undo_log 生成反向 SQL;<br>    UPDATE product SET stock = 100 WHERE id = 1;<br>&#125;<br></code></pre></td></tr></table></figure><p>AT 模式是典型的“低门槛”方案，但工程界没有免费的午餐。它有一个致命隐患：<strong>脏写（Dirty Write）</strong>。</p><p>如果有一个非 Seata 管理的后台脚本，直接去修改了数据库，会发生什么？</p><ol><li>事务 A 修改库存 ，提交本地事务（释放了数据库行锁）。</li><li><strong>外部脚本</strong> 修改库存 。</li><li>事务 A 收到回滚指令，根据 Before Image (100) 强行回滚：<code>SET stock = 100</code>。</li><li><strong>结果</strong>：外部脚本修改的 80 被覆盖丢失了。</li></ol><p>为了解决这个问题，Seata 引入了 <strong>全局锁</strong>。在二阶段提交&#x2F;回滚完成前，该记录被 Seata 的协调者（TC）逻辑锁定。这带来了一个严重的性能权衡：<strong>热点数据的高并发写</strong>。</p><p>如果你的业务是“秒杀”场景，成千上万的请求同时抢夺同一行库存记录的全局锁，Seata AT 将成为系统的性能瓶颈，且极易引发死锁。</p><p><strong>结论</strong>：AT 适合并发量适中、非热点数据、追求开发效率的中后台业务。</p><h3 id="XA-模式"><a href="#XA-模式" class="headerlink" title="XA 模式"></a>XA 模式</h3><p>在 AT 模式中，Seata 需要自己在应用层解析 SQL、生成 Undo Log，这就像是一个“外挂”。</p><p>而 <strong>XA 模式</strong> 是数据库（MySQL、Oracle、PostgreSQL）<strong>自带</strong> 的功能。它遵循 X&#x2F;Open 组织制定的 DTP（Distributed Transaction Processing）标准。</p><p>类比：正规的法律合同</p><ul><li><strong>AT 模式</strong>：是你和朋友私下约定的“君子协定”，如果反悔了，你们自己负责恢复现场。</li><li><strong>XA 模式</strong>：是走了公证处的“法律合同”。数据库本身（公证处）保证：只要你签了字（Prepare），我就确保这件事一定能成，或者一定能退，不需要你自己操心怎么退。</li></ul><p>XA 是 <strong>2PC（两阶段提交）</strong> 的标准实现。</p><ul><li><strong>一阶段（Prepare）</strong>：业务代码执行 SQL，但在提交时，Seata（TM）会告诉数据库：“先别真提交，把资源锁死，数据写入日志，进入‘准备好’的状态”。此时，数据库会锁定这一行数据，任何人都动不了。</li><li><strong>二阶段（Commit &#x2F; Rollback）</strong>：Seata 收集所有服务的状态。<ul><li><strong>全成功</strong>：通知数据库“正式提交”，数据库释放锁，数据生效。</li><li><strong>有失败</strong>：通知数据库“回滚”，数据库利用自身的日志恢复，释放锁。</li></ul></li></ul><p>Seata XA 与 Seata AT 的核心区别</p><ul><li><strong>AT</strong>：锁在 Seata 侧（Global Lock），数据库本地事务早就提交了。回滚靠 Undo Log。</li><li><strong>XA</strong>：锁在数据库侧，数据库本地事务一直 <strong>挂起</strong>，直到二阶段结束。</li></ul><p>优点：</p><ol><li><strong>强一致性</strong>：由数据库保证，没有脏读，没有中间状态。</li><li><strong>SQL 支持度完美</strong>：AT 模式解析复杂 SQL（如存储过程、复杂连接）可能会失败，但 XA 是数据库原生的，支持所有 SQL。</li><li><strong>无侵入</strong>：不需要像 AT 那样建 <code>undo_log</code> 表。</li></ol><p>缺点：</p><ol><li><strong>性能短板</strong>：因为数据库锁要持有到第二阶段结束（跨网络通讯），锁的时间长，并发度低。</li><li><strong>木桶效应</strong>：如果协调者（TC）挂了，或者网络断了，数据库的锁会一直释放不掉，导致业务瘫痪。</li></ol><p>为什么它又“重获新生”了？</p><p>过去互联网公司不用 XA 是因为性能差。但随着 <strong>云原生数据库</strong>（如阿里云 PolarDB）和 <strong>高速网络</strong> 的普及，XA 的性能损耗被大幅降低。现在，Seata 的 XA 模式已经优化得非常好了，对于 <strong>非高并发、但数据极其重要</strong> 的系统（如内部 ERP、财务系统），XA 是最稳妥的选择。</p><h3 id="Seata-TCC-模式"><a href="#Seata-TCC-模式" class="headerlink" title="Seata TCC 模式"></a>Seata TCC 模式</h3><p>AT 模式因为要加全局锁，性能有上限。为了追求极致性能，我们需要把锁的粒度从“数据库层面”移交到“业务逻辑层面”。</p><p>类比：买房交定金</p><ul><li><strong>Try (定金)</strong>：你不用付全款，先付 5 万定金。房产中介把房子“锁”住（预留），别人买不了。</li><li><strong>Confirm (交房)</strong>：你凑齐了首付，正式过户。</li><li><strong>Cancel (退定)</strong>：你反悔了，中介把房子释放出来卖给别人，定金退回。</li></ul><p>TCC 强制要求你将一个业务接口拆分为三个方法。</p><p>代码逻辑：</p><figure class="highlight java"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br><span class="line">17</span><br><span class="line">18</span><br><span class="line">19</span><br><span class="line">20</span><br><span class="line">21</span><br><span class="line">22</span><br><span class="line">23</span><br></pre></td><td class="code"><pre><code class="hljs java"><span class="hljs-keyword">public</span> <span class="hljs-keyword">interface</span> <span class="hljs-title class_">StockService</span> &#123;<br>    <span class="hljs-comment">// 1. Try: 资源检查 + 预留</span><br>    <span class="hljs-comment">// 并不是直接扣减，而是把库存移入 &quot;frozen_stock&quot; 字段</span><br>    <span class="hljs-meta">@Transactional</span><br>    <span class="hljs-keyword">void</span> <span class="hljs-title function_">tryDeduct</span><span class="hljs-params">(<span class="hljs-type">long</span> id, <span class="hljs-type">int</span> count)</span> &#123;<br>        checkStock(id, count);<br>        stock -= count;<br>        frozen_stock += count; <span class="hljs-comment">// 关键：冻结资源</span><br>    &#125;<br><br>    <span class="hljs-comment">// 2. Confirm: 真正扣减</span><br>    <span class="hljs-comment">// 此时不需要检查库存，直接使用 Try 阶段锁定的资源</span><br>    <span class="hljs-keyword">void</span> <span class="hljs-title function_">confirmDeduct</span><span class="hljs-params">(<span class="hljs-type">long</span> id, <span class="hljs-type">int</span> count)</span> &#123;<br>        frozen_stock -= count; <span class="hljs-comment">// 消耗冻结，真正完成</span><br>    &#125;<br><br>    <span class="hljs-comment">// 3. Cancel: 释放资源</span><br>    <span class="hljs-keyword">void</span> <span class="hljs-title function_">cancelDeduct</span><span class="hljs-params">(<span class="hljs-type">long</span> id, <span class="hljs-type">int</span> count)</span> &#123;<br>        stock += count;        <span class="hljs-comment">// 恢复可用库存</span><br>        frozen_stock -= count; <span class="hljs-comment">// 释放冻结</span><br>    &#125;<br>&#125;<br><br></code></pre></td></tr></table></figure><p>TCC 赋予了开发者极大的灵活性，但它将 <strong>复杂性</strong> 完全转嫁给了业务方。在分布式网络环境中，你必须在代码中处理以下三种极端异常，否则会导致资金损失。</p><ol><li><p><strong>空回滚</strong></p><ul><li><p>场景：Try 请求因为网络丢包，根本没发到库存服务。但协调器（TC）认为失败了，触发了 Cancel。</p></li><li><p>后果：Cancel 执行了“退钱&#x2F;释放库存”的操作。如果没做防护，会导致库存凭空增加。</p></li><li><p>对策：Cancel 必须检查“是否收到过 Try”。</p></li></ul></li><li><p><strong>幂等</strong></p><ul><li><p>场景：网络抖动，TC 以为 Confirm 超时了，重发了一次 Confirm。</p></li><li><p>后果：库存被扣了两次。</p></li><li><p>对策：所有 Confirm&#x2F;Cancel 必须实现幂等，处理过的事务 ID 直接返回成功。</p></li></ul></li><li><p><strong>悬挂</strong></p><ul><li>场景：网络极度拥堵。Cancel 比 Try 先到了。Cancel 发现没 Try 过，直接返回成功（空回滚）。随后，迟到的 Try 到了，并执行成功。</li><li>后果：资源被 Try 冻结了，但永远不会被 Confirm 或 Cancel，变成了“僵尸资源”。</li><li>对策：Try 执行时，必须检查该事务 ID 是否已经执行过 Cancel。</li></ul></li></ol><p><strong>结论</strong>：TCC 性能极高（不长期持有数据库锁），但开发和测试成本极高。仅适用于核心资金链路或对并发要求极高的场景。</p><h3 id="Seata-Saga-模式"><a href="#Seata-Saga-模式" class="headerlink" title="Seata Saga 模式"></a>Seata Saga 模式</h3><p>有些业务流程非常长，既不能像 AT 模式那样长时间锁表（导致数据库瘫痪），也不能像 TCC 那样要求每个服务都改造代码。</p><p>你要预订一次欧洲旅行，流程是：<code>订机票</code>  <code>订酒店</code>  <code>租车</code>。这个流程涉及三个不同的供应商，甚至可能需要人工审核，整个过程可能持续几分钟甚至几小时。</p><ul><li><strong>如果用 AT&#x2F;TCC</strong>：从订机票开始，数据库就被锁住了。直到租车成功，机票的锁才释放。这期间，别人无法买这张票。</li></ul><p>核心定义：Saga 模式把一个大事务拆分成多个 <strong>互不相关</strong> 的本地短事务。就像接力赛：</p><ul><li>第一棒（机票）跑完，<strong>立刻提交</strong>，释放锁。</li><li>第二棒（酒店）跑完，<strong>立刻提交</strong>。</li><li>如果第三棒（租车）摔倒了，Saga 协调器会负责往回跑：退酒店（补偿）、退机票（补偿）。</li></ul><p>Saga 的核心在于定义“如何前进”和“如何后退”。开发者需要通过 JSON 或代码定义一个状态机。</p><ul><li><strong>正常情况（All Good）：</strong>（提交）  （提交）  （提交）  <strong>结束</strong></li><li><strong>异常情况（C 失败）：</strong>（提交）  （提交）  （失败）  <strong>触发回滚</strong>  （B 的补偿）  （A 的补偿）</li></ul><p>Saga 是分布式事务中最“反直觉”的模式，因为它打破了 ACID 中的 <strong>I (Isolation，隔离性)</strong>。</p><p>由于缺乏隔离性引发的“脏读。在 Saga 模式中，因为每个子事务都是 <strong>立即提交</strong> 的，所以中间状态对外界是 <strong>可见</strong> 的。</p><ol><li>用户 A 订了最后一张机票（ 提交）。</li><li>用户 B 来查票，发现没票了（看见了 A 的提交）。</li><li>用户 A 后续的酒店预订失败了，触发补偿取消了机票（ 执行）。</li><li><strong>结果</strong>：用户 B 刚才看到的是一个“无效”的状态，且因为这个状态放弃了购买。这就是 <strong>脏读</strong>。</li></ol><p>为了解决这个问题，你通常需要在业务层面引入 <strong>“语义锁” (Semantic Lock)</strong>。</p><ul><li><em>比如</em>：在订机票时，不直接把票减掉，而是将状态改为 <code>BOOKING_PENDING</code>（预订中）。</li><li>只有当整个 Saga 流程全部完成，才把状态改为 <code>CONFIRMED</code>。</li></ul><p><strong>结论</strong>：Saga 适合 <strong>业务流程极长</strong>、<strong>跨机构&#x2F;跨遗留系统</strong>（无法改造代码使用 TCC，只能调用接口）的场景。</p><h3 id="RocketMQ-事务消息"><a href="#RocketMQ-事务消息" class="headerlink" title="RocketMQ 事务消息"></a>RocketMQ 事务消息</h3><p>并不是所有业务都需要“同时成功”。</p><p><strong>场景</strong>：用户支付成功后，需要增加用户积分。</p><p>如果支付成功了，积分服务挂了，用户能不能忍受积分晚 10 秒到账？通常是可以的。</p><p>这就不需要强一致性事务，而是 <strong>最终一致性</strong>。核心思想是：<strong>只要我本地事务成功了，我就保证消息一定能发出去；只要消息发出去了，消费者最终一定能收到。</strong></p><p>普通的 MQ 发送消息是不可靠的：可能消息发出去了，本地数据库回滚了；或者本地数据库提交了，消息发失败了。RocketMQ 引入了 <strong>Half Message（半消息）</strong> 来解决原子性问题。</p><p><strong>核心步骤：</strong></p><ol><li><p><strong>发送半消息</strong>：生产者发一条消息给 MQ，MQ 收到了但不给消费者看（Pending 状态）。</p></li><li><p><strong>执行本地事务</strong>：生产者执行具体的业务（如：更新订单状态）。</p></li><li><p><strong>Commit&#x2F;Rollback</strong>：</p><ul><li><p>本地事务成功  通知 MQ “这条消息可以投递了”。</p></li><li><p>本地事务失败  通知 MQ “把这条消息删了吧”。</p></li></ul></li></ol><p>RocketMQ 方案的精髓在于 <strong>处理“未决”状态</strong>。</p><p>如果在执行本地事务后，服务器立刻断电，没来得及发 Commit 给 MQ，怎么办？</p><p>MQ 里的消息一直是“半消息”状态。RocketMQ 会启动 <strong>回查机制（CheckBack）</strong>。</p><p>MQ 会主动问生产者：“刚才那个事务 ID，你到底执行成功没？”</p><p>生产者需要实现一个检查接口（例如查数据库订单表），确认事务状态并回复。</p><p><strong>本地消息表 (Local Message Table)</strong>：如果你不用 RocketMQ，也可以用“本地消息表”模式。原理一样：在同一个数据库事务里插入业务数据和一条“待发送”的消息记录。然后有一个定时任务不断轮询这张表去发消息。</p><ul><li><strong>代价</strong>：不仅要写业务表，还要写消息表，存在数据库 I&#x2F;O 竞争。</li></ul><p><strong>结论</strong>：这是微服务解耦最常用的方案。它牺牲了实时性（E），换取了高可用（A）和分区容错性（P）。</p>]]></content>
    
    
    <categories>
      
      <category>架构设计</category>
      
    </categories>
    
    
    <tags>
      
      <tag>分布式事务</tag>
      
      <tag>微服务架构</tag>
      
      <tag>CAP定理</tag>
      
    </tags>
    
  </entry>
  
  
  
  
</search>
